---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Activation CR only — apply AFTER the kueue-operator CSV reaches Succeeded.
# See ../README.md and kueue.yaml.
resources:
  - kueue.yaml
