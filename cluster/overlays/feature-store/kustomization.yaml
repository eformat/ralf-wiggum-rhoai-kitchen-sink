---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Feature Store (Feast-based). Labs mapped here: feature-store-feast.
# The FeatureStore CR and PostgreSQL-backed persistence are lab exercises.
resources:
  - ../../base
patches:
  - path: dsc-patch.yaml
