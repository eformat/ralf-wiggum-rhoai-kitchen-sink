---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Activation CRs only — apply AFTER the JobSet and LWS CSVs reach Succeeded
# (the CRDs these CRs use do not exist until then). See ../README.md.
resources:
  - operator-config.yaml
