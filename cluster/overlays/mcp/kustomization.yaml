---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MCP Lifecycle Operator. Labs mapped here: mcp-lifecycle-operator,
# mcp-catalog-support-tier, mcp-catalog-admin, mcp-gateway-operator (with the
# gateway overlay — the MCP gateway Operator itself installs in-lab via OLM).
resources:
  - ../../base
patches:
  - path: dsc-patch.yaml
