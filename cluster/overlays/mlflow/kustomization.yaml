---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# MLflow experiment tracking / MLOps. Labs mapped here:
# mlflow-experiment-tracking, opencode-coding-agent (tracing),
# genai-studio-saved-agent (saved agents reference MLflow prompts).
#
# NOTE: the cluster-scoped MLflow instance (exactly one, named `mlflow`, in
# redhat-ods-applications) is a lab exercise — this overlay only enables the
# operator component.
resources:
  - ../../base
patches:
  - path: dsc-patch.yaml
