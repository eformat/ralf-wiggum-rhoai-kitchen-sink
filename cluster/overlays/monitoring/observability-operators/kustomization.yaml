---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Observability backend operators: Cluster Observability Operator + Red Hat
# build of OpenTelemetry (subscriptions only; both AllNamespaces install mode).
resources:
  - coo-namespace.yaml
  - coo-operatorgroup.yaml
  - coo-subscription.yaml
  - otel-namespace.yaml
  - otel-operatorgroup.yaml
  - otel-subscription.yaml
