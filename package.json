{
  "name": "ralf-wiggum-rhoai-kitchen-sink",
  "version": "1.0.0",
  "description": "RHOAI kitchen sink workshop catalog - one workshop per feature",
  "private": true,
  "scripts": {
    "build": "antora site.yml --stacktrace",
    "serve": "python3 -m http.server 8887 --directory www"
  },
  "devDependencies": {
    "@antora/cli": "^3.1.0",
    "@antora/site-generator-default": "^3.1.0"
  },
  "dependencies": {
    "opencode-ai": "^1.18.31"
  }
}
