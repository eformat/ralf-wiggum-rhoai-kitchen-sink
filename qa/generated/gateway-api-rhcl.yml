# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: gateway-api-rhcl (37 runnable blocks, 10 skipped)
- name: "Lab gateway-api-rhcl"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/gateway-api-rhcl/work"
    logdir: "{{ playbook_dir }}/../runs/gateway-api-rhcl"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-concepts.adoc | Exercise 2 Inspect the installation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export KUADRANT_SYSTEM_NS=$(oc get kuadrant -A -o jsonpath="{.items[0].metadata.namespace}")
            echo $KUADRANT_SYSTEM_NS
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
        - name: "04 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-04.sh" <<'QAENV'
            export KUADRANT_SYSTEM_NS=$(oc get kuadrant -A -o jsonpath="{.items[0].metadata.namespace}")
            QAENV
            cat "{{ logdir }}/exports-04.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the installation
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-concepts.adoc | Exercise 2 Inspect the installation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc wait kuadrant/kuadrant --for="condition=Ready=true" -n ${KUADRANT_SYSTEM_NS} --timeout=300s
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the installation
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-concepts.adoc | Exercise 2 Inspect the installation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get pods -n ${KUADRANT_SYSTEM_NS}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the installation
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-02-hands-on.adoc | Exercise 1 Set up your environment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export KUADRANT_GATEWAY_NS=abc123-gateway
            export KUADRANT_GATEWAY_NAME=ingress-gateway
            export KUADRANT_DEVELOPER_NS=abc123-user1
            export KUADRANT_ZONE_ROOT_DOMAIN=example.com
            export KUADRANT_CLUSTER_ISSUER_NAME=self-signed
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
        - name: "07 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-07.sh" <<'QAENV'
            export KUADRANT_GATEWAY_NS=abc123-gateway
            export KUADRANT_GATEWAY_NAME=ingress-gateway
            export KUADRANT_DEVELOPER_NS=abc123-user1
            export KUADRANT_ZONE_ROOT_DOMAIN=example.com
            export KUADRANT_CLUSTER_ISSUER_NAME=self-signed
            QAENV
            cat "{{ logdir }}/exports-07.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Set up your environment
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-02-hands-on.adoc | Exercise 1 Set up your environment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project ${KUADRANT_GATEWAY_NS}
            oc apply -f https://raw.githubusercontent.com/Kuadrant/Kuadrantoperator/main/examples/toystore/toystore.yaml -n ${KUADRANT_DEVELOPER_NS}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Set up your environment
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get pods -n ${KUADRANT_DEVELOPER_NS}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-02-hands-on.adoc | Exercise 2 Create your Gateway object"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: gateway.networking.k8s.io/v1
            kind: Gateway
            metadata:
              name: ${KUADRANT_GATEWAY_NAME}
              namespace: ${KUADRANT_GATEWAY_NS}
              labels:
                kuadrant.io/gateway: "true"
            spec:
              gatewayClassName: openshift-default
              listeners:
                - allowedRoutes:
                    namespaces:
                      from: All
                  hostname: "api.${KUADRANT_ZONE_ROOT_DOMAIN}"
                  name: api
                  port: 443
                  protocol: HTTPS
                  tls:
                    certificateRefs:
                      - group: ""
                        kind: Secret
                        name: api-${KUADRANT_GATEWAY_NAME}-tls
                    mode: Terminate
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Create your Gateway object
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
    - block:
        - name: "11 | module-02-hands-on.adoc | Exercise 2 Create your Gateway object"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gateway ${KUADRANT_GATEWAY_NAME} -n ${KUADRANT_GATEWAY_NS} -o=jsonpath='{.status.conditions[?(@.type=="Accepted")].message}{"\n"}{.status.conditions[?(@.type=="Programmed")].message}{"\n"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r11
          changed_when: false
          failed_when:
            - r11.rc != 0
            - "'AlreadyExists' not in (r11.stderr | default(''))"
            - "'already exists' not in (r11.stderr | default(''))"
            - "'NotFound' not in (r11.stderr | default(''))"
      always:
        - name: "11 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Create your Gateway object
              {{ r11.rc | default('?') }}
              {{ r11.stdout | default('') }}
              {{ r11.stderr | default('') }}
            dest: "{{ logdir }}/11.log"
    - block:
        - name: "12 | module-02-hands-on.adoc | Exercise 3 Secure the Gateway with TLS"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: cert-manager.io/v1
            kind: ClusterIssuer
            metadata:
              name: ${KUADRANT_CLUSTER_ISSUER_NAME}
            spec:
              selfSigned: {}
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r12
          changed_when: false
          failed_when:
            - r12.rc != 0
            - "'AlreadyExists' not in (r12.stderr | default(''))"
            - "'already exists' not in (r12.stderr | default(''))"
            - "'NotFound' not in (r12.stderr | default(''))"
      always:
        - name: "12 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Secure the Gateway with TLS
              {{ r12.rc | default('?') }}
              {{ r12.stdout | default('') }}
              {{ r12.stderr | default('') }}
            dest: "{{ logdir }}/12.log"
    - block:
        - name: "13 | module-02-hands-on.adoc | Exercise 3 Secure the Gateway with TLS"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: kuadrant.io/v1
            kind: TLSPolicy
            metadata:
              name: ${KUADRANT_GATEWAY_NAME}-tls
              namespace: ${KUADRANT_GATEWAY_NS}
            spec:
              targetRef:
                name: ${KUADRANT_GATEWAY_NAME}
                group: gateway.networking.k8s.io
                kind: Gateway
              issuerRef:
                group: cert-manager.io
                kind: ClusterIssuer
                name: ${KUADRANT_CLUSTER_ISSUER_NAME}
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r13
          changed_when: false
          failed_when:
            - r13.rc != 0
            - "'AlreadyExists' not in (r13.stderr | default(''))"
            - "'already exists' not in (r13.stderr | default(''))"
            - "'NotFound' not in (r13.stderr | default(''))"
      always:
        - name: "13 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Secure the Gateway with TLS
              {{ r13.rc | default('?') }}
              {{ r13.stdout | default('') }}
              {{ r13.stderr | default('') }}
            dest: "{{ logdir }}/13.log"
    - block:
        - name: "14 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get tlspolicy ${KUADRANT_GATEWAY_NAME}-tls -n ${KUADRANT_GATEWAY_NS} -o=jsonpath='{.status.conditions[?(@.type=="Accepted")].message}{"\n"}{.status.conditions[?(@.type=="Enforced")].message}{"\n"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r14
          changed_when: false
          failed_when:
            - r14.rc != 0
            - "'AlreadyExists' not in (r14.stderr | default(''))"
            - "'already exists' not in (r14.stderr | default(''))"
            - "'NotFound' not in (r14.stderr | default(''))"
      always:
        - name: "14 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r14.rc | default('?') }}
              {{ r14.stdout | default('') }}
              {{ r14.stderr | default('') }}
            dest: "{{ logdir }}/14.log"
    - block:
        - name: "15 | module-02-hands-on.adoc | Exercise 4 Publish the Toystore API with an HTTPRoute"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: gateway.networking.k8s.io/v1
            kind: HTTPRoute
            metadata:
              name: toystore
              namespace: ${KUADRANT_DEVELOPER_NS}
              labels:
                deployment: toystore
                service: toystore
            spec:
              parentRefs:
                - name: ${KUADRANT_GATEWAY_NAME}
                  namespace: ${KUADRANT_GATEWAY_NS}
              hostnames:
                - api.${KUADRANT_ZONE_ROOT_DOMAIN}
              rules:
                - matches:
                    - method: GET
                      path:
                        type: PathPrefix
                        value: /cars
                    - method: GET
                      path:
                        type: PathPrefix
                        value: /health
                  backendRefs:
                    - name: toystore
                      port: 80
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r15
          changed_when: false
          failed_when:
            - r15.rc != 0
            - "'AlreadyExists' not in (r15.stderr | default(''))"
            - "'already exists' not in (r15.stderr | default(''))"
            - "'NotFound' not in (r15.stderr | default(''))"
      always:
        - name: "15 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Publish the Toystore API with an HTTPRoute
              {{ r15.rc | default('?') }}
              {{ r15.stdout | default('') }}
              {{ r15.stderr | default('') }}
            dest: "{{ logdir }}/15.log"
    - block:
        - name: "16 | module-02-hands-on.adoc | Exercise 4 Publish the Toystore API with an HTTPRoute"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            # --resolve needs an IP: on clusters where the gateway status address is a
            # hostname (no LoadBalancer provider), use the gateway Service ClusterIP
            export GATEWAY_ADDRESS=$(oc get svc ${KUADRANT_GATEWAY_NAME}-openshift-default -n ${KUADRANT_GATEWAY_NS} -o jsonpath='{.spec.clusterIP}')
            echo $GATEWAY_ADDRESS
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r16
          changed_when: false
          failed_when:
            - r16.rc != 0
            - "'AlreadyExists' not in (r16.stderr | default(''))"
            - "'already exists' not in (r16.stderr | default(''))"
            - "'NotFound' not in (r16.stderr | default(''))"
        - name: "16 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-16.sh" <<'QAENV'
            export GATEWAY_ADDRESS=$(oc get svc ${KUADRANT_GATEWAY_NAME}-openshift-default -n ${KUADRANT_GATEWAY_NS} -o jsonpath='{.spec.clusterIP}')
            QAENV
            cat "{{ logdir }}/exports-16.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "16 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Publish the Toystore API with an HTTPRoute
              {{ r16.rc | default('?') }}
              {{ r16.stdout | default('') }}
              {{ r16.stderr | default('') }}
            dest: "{{ logdir }}/16.log"
    - block:
        - name: "17 | module-02-hands-on.adoc | Exercise 4 Publish the Toystore API with an HTTPRoute"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -k --resolve "api.${KUADRANT_ZONE_ROOT_DOMAIN}:443:${GATEWAY_ADDRESS}" "https://api.${KUADRANT_ZONE_ROOT_DOMAIN}/cars"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r17
          changed_when: false
          failed_when:
            - r17.rc != 0
            - "'AlreadyExists' not in (r17.stderr | default(''))"
            - "'already exists' not in (r17.stderr | default(''))"
            - "'NotFound' not in (r17.stderr | default(''))"
      always:
        - name: "17 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Publish the Toystore API with an HTTPRoute
              {{ r17.rc | default('?') }}
              {{ r17.stdout | default('') }}
              {{ r17.stderr | default('') }}
            dest: "{{ logdir }}/17.log"
    - block:
        - name: "18 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get httproute toystore -n ${KUADRANT_DEVELOPER_NS} -o=jsonpath='{.status.parents[].conditions[?(@.type=="Accepted")].message}{"\n"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r18
          changed_when: false
          failed_when:
            - r18.rc != 0
            - "'AlreadyExists' not in (r18.stderr | default(''))"
            - "'already exists' not in (r18.stderr | default(''))"
            - "'NotFound' not in (r18.stderr | default(''))"
      always:
        - name: "18 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r18.rc | default('?') }}
              {{ r18.stdout | default('') }}
              {{ r18.stderr | default('') }}
            dest: "{{ logdir }}/18.log"
    - block:
        - name: "19 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: kuadrant.io/v1
            kind: AuthPolicy
            metadata:
              name: ${KUADRANT_GATEWAY_NAME}-auth
              namespace: ${KUADRANT_GATEWAY_NS}
            spec:
              targetRef:
                group: gateway.networking.k8s.io
                kind: Gateway
                name: ${KUADRANT_GATEWAY_NAME}
              defaults:
                when:
                  - predicate: "request.path != '/health'"
                rules:
                  authorization:
                    deny-all:
                      opa:
                        rego: "allow = false"
                response:
                  unauthorized:
                    headers:
                      "content-type":
                        value: application/json
                    body:
                      value: |
                        {
                          "error": "Forbidden",
                          "message": "Access denied by default by the gateway operator. If you are the administrator of the service, create a specific auth policy for the route."
                        }
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r19
          changed_when: false
          failed_when:
            - r19.rc != 0
            - "'AlreadyExists' not in (r19.stderr | default(''))"
            - "'already exists' not in (r19.stderr | default(''))"
            - "'NotFound' not in (r19.stderr | default(''))"
      always:
        - name: "19 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r19.rc | default('?') }}
              {{ r19.stdout | default('') }}
              {{ r19.stderr | default('') }}
            dest: "{{ logdir }}/19.log"
    - block:
        - name: "20 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: kuadrant.io/v1
            kind: RateLimitPolicy
            metadata:
              name: ${KUADRANT_GATEWAY_NAME}-rlp
              namespace: ${KUADRANT_GATEWAY_NS}
            spec:
              targetRef:
                group: gateway.networking.k8s.io
                kind: Gateway
                name: ${KUADRANT_GATEWAY_NAME}
              defaults:
                limits:
                  "low-limit":
                    rates:
                      - limit: 1
                        window: 10s
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r20
          changed_when: false
          failed_when:
            - r20.rc != 0
            - "'AlreadyExists' not in (r20.stderr | default(''))"
            - "'already exists' not in (r20.stderr | default(''))"
            - "'NotFound' not in (r20.stderr | default(''))"
      always:
        - name: "20 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r20.rc | default('?') }}
              {{ r20.stdout | default('') }}
              {{ r20.stderr | default('') }}
            dest: "{{ logdir }}/20.log"
    - block:
        - name: "21 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get authpolicy ${KUADRANT_GATEWAY_NAME}-auth -n ${KUADRANT_GATEWAY_NS} -o=jsonpath='{.status.conditions[?(@.type=="Accepted")].message}{"\n"}{.status.conditions[?(@.type=="Enforced")].message}{"\n"}'
            oc get ratelimitpolicy ${KUADRANT_GATEWAY_NAME}-rlp -n ${KUADRANT_GATEWAY_NS} -o=jsonpath='{.status.conditions[?(@.type=="Accepted")].message}{"\n"}{.status.conditions[?(@.type=="Enforced")].message}{"\n"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r21
          changed_when: false
          failed_when:
            - r21.rc != 0
            - "'AlreadyExists' not in (r21.stderr | default(''))"
            - "'already exists' not in (r21.stderr | default(''))"
            - "'NotFound' not in (r21.stderr | default(''))"
      always:
        - name: "21 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r21.rc | default('?') }}
              {{ r21.stdout | default('') }}
              {{ r21.stderr | default('') }}
            dest: "{{ logdir }}/21.log"
    - block:
        - name: "22 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -k --resolve "api.${KUADRANT_ZONE_ROOT_DOMAIN}:443:${GATEWAY_ADDRESS}" "https://api.${KUADRANT_ZONE_ROOT_DOMAIN}/cars"
            curl -k --resolve "api.${KUADRANT_ZONE_ROOT_DOMAIN}:443:${GATEWAY_ADDRESS}" "https://api.${KUADRANT_ZONE_ROOT_DOMAIN}/health"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r22
          changed_when: false
          failed_when:
            - r22.rc != 0
            - "'AlreadyExists' not in (r22.stderr | default(''))"
            - "'already exists' not in (r22.stderr | default(''))"
            - "'NotFound' not in (r22.stderr | default(''))"
      always:
        - name: "22 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r22.rc | default('?') }}
              {{ r22.stdout | default('') }}
              {{ r22.stderr | default('') }}
            dest: "{{ logdir }}/22.log"
    - block:
        - name: "23 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export KUADRANT_SYSTEM_NS=$(oc get kuadrant -A -o jsonpath="{.items[0].metadata.namespace}")
            cat <<EOF | oc apply -f -
            apiVersion: v1
            kind: Secret
            metadata:
              name: bob-key
              namespace: ${KUADRANT_SYSTEM_NS}
              labels:
                authorino.kuadrant.io/managed-by: authorino
                app: toystore
              annotations:
                secret.kuadrant.io/user-id: bob
            stringData:
              api_key: IAMBOB
            type: Opaque
            ---
            apiVersion: v1
            kind: Secret
            metadata:
              name: alice-key
              namespace: ${KUADRANT_SYSTEM_NS}
              labels:
                authorino.kuadrant.io/managed-by: authorino
                app: toystore
              annotations:
                secret.kuadrant.io/user-id: alice
            stringData:
              api_key: IAMALICE
            type: Opaque
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r23
          changed_when: false
          failed_when:
            - r23.rc != 0
            - "'AlreadyExists' not in (r23.stderr | default(''))"
            - "'already exists' not in (r23.stderr | default(''))"
            - "'NotFound' not in (r23.stderr | default(''))"
        - name: "23 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-23.sh" <<'QAENV'
            export KUADRANT_SYSTEM_NS=$(oc get kuadrant -A -o jsonpath="{.items[0].metadata.namespace}")
            QAENV
            cat "{{ logdir }}/exports-23.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "23 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r23.rc | default('?') }}
              {{ r23.stdout | default('') }}
              {{ r23.stderr | default('') }}
            dest: "{{ logdir }}/23.log"
    - block:
        - name: "24 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: kuadrant.io/v1
            kind: AuthPolicy
            metadata:
              name: toystore-auth
              namespace: ${KUADRANT_DEVELOPER_NS}
            spec:
              targetRef:
                group: gateway.networking.k8s.io
                kind: HTTPRoute
                name: toystore
              defaults:
                when:
                  - predicate: "request.path != '/health'"
                rules:
                  authentication:
                    "api-key-users":
                      apiKey:
                        selector:
                          matchLabels:
                            app: toystore
                        credentials:
                          authorizationHeader:
                            prefix: APIKEY
                  response:
                    success:
                      filters:
                        "identity":
                          json:
                            properties:
                              "userid":
                                selector: auth.identity.metadata.annotations.secret\.kuadrant\.io/user-id
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r24
          changed_when: false
          failed_when:
            - r24.rc != 0
            - "'AlreadyExists' not in (r24.stderr | default(''))"
            - "'already exists' not in (r24.stderr | default(''))"
            - "'NotFound' not in (r24.stderr | default(''))"
      always:
        - name: "24 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r24.rc | default('?') }}
              {{ r24.stdout | default('') }}
              {{ r24.stderr | default('') }}
            dest: "{{ logdir }}/24.log"
    - block:
        - name: "25 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: kuadrant.io/v1
            kind: RateLimitPolicy
            metadata:
              name: toystore-rlp
              namespace: ${KUADRANT_DEVELOPER_NS}
            spec:
              targetRef:
                group: gateway.networking.k8s.io
                kind: HTTPRoute
                name: toystore
              limits:
                "general-user":
                  rates:
                    - limit: 5
                      window: 10s
                  counters:
                    - expression: auth.identity.userid
                  when:
                    - predicate: "auth.identity.userid != 'bob'"
                "bob-limit":
                  rates:
                    - limit: 2
                      window: 10s
                  when:
                    - predicate: "auth.identity.userid == 'bob'"
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r25
          changed_when: false
          failed_when:
            - r25.rc != 0
            - "'AlreadyExists' not in (r25.stderr | default(''))"
            - "'already exists' not in (r25.stderr | default(''))"
            - "'NotFound' not in (r25.stderr | default(''))"
      always:
        - name: "25 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r25.rc | default('?') }}
              {{ r25.stdout | default('') }}
              {{ r25.stderr | default('') }}
            dest: "{{ logdir }}/25.log"
    - block:
        - name: "26 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get ratelimitpolicy toystore-rlp -n ${KUADRANT_DEVELOPER_NS} -o=jsonpath='{.status.conditions[?(@.type=="Accepted")].message}{"\n"}{.status.conditions[?(@.type=="Enforced")].message}{"\n"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r26
          changed_when: false
          failed_when:
            - r26.rc != 0
            - "'AlreadyExists' not in (r26.stderr | default(''))"
            - "'already exists' not in (r26.stderr | default(''))"
            - "'NotFound' not in (r26.stderr | default(''))"
      always:
        - name: "26 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r26.rc | default('?') }}
              {{ r26.stdout | default('') }}
              {{ r26.stderr | default('') }}
            dest: "{{ logdir }}/26.log"
    - block:
        - name: "27 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            while :; do curl -k --write-out '%{http_code}\n' --silent --output /dev/null --resolve "api.${KUADRANT_ZONE_ROOT_DOMAIN}:443:${GATEWAY_ADDRESS}" -H 'Authorization: APIKEY IAMALICE' "https://api.${KUADRANT_ZONE_ROOT_DOMAIN}/cars" | grep -E --color "\b(429)\b|$"; sleep 1; done
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r27
          changed_when: false
          failed_when:
            - r27.rc != 0
            - "'AlreadyExists' not in (r27.stderr | default(''))"
            - "'already exists' not in (r27.stderr | default(''))"
            - "'NotFound' not in (r27.stderr | default(''))"
      always:
        - name: "27 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r27.rc | default('?') }}
              {{ r27.stdout | default('') }}
              {{ r27.stderr | default('') }}
            dest: "{{ logdir }}/27.log"
    - block:
        - name: "28 | module-02-hands-on.adoc | Exercise 5 Protect the API with AuthPolicy and RateLimitPoli"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            while :; do curl -k --write-out '%{http_code}\n' --silent --output /dev/null --resolve "api.${KUADRANT_ZONE_ROOT_DOMAIN}:443:${GATEWAY_ADDRESS}" -H 'Authorization: APIKEY IAMBOB' "https://api.${KUADRANT_ZONE_ROOT_DOMAIN}/cars" | grep -E --color "\b(429)\b|$"; sleep 1; done
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r28
          changed_when: false
          failed_when:
            - r28.rc != 0
            - "'AlreadyExists' not in (r28.stderr | default(''))"
            - "'already exists' not in (r28.stderr | default(''))"
            - "'NotFound' not in (r28.stderr | default(''))"
      always:
        - name: "28 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 5: Protect the API with AuthPolicy and RateLimitPolicy
              {{ r28.rc | default('?') }}
              {{ r28.stdout | default('') }}
              {{ r28.stderr | default('') }}
            dest: "{{ logdir }}/28.log"
    - block:
        - name: "29 | module-03-advanced.adoc | Module 03 Advanced Usage"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export KUADRANT_GATEWAY_NS=abc123-gateway
            export KUADRANT_GATEWAY_NAME=ingress-gateway
            export KUADRANT_DEVELOPER_NS=abc123-user1
            export KUADRANT_ZONE_ROOT_DOMAIN=example.com
            export KUADRANT_SYSTEM_NS=$(oc get kuadrant -A -o jsonpath="{.items[0].metadata.namespace}")
            # --resolve needs an IP: on clusters where the gateway status address is a
            # hostname (no LoadBalancer provider), use the gateway Service ClusterIP
            export GATEWAY_ADDRESS=$(oc get svc ${KUADRANT_GATEWAY_NAME}-openshift-default -n ${KUADRANT_GATEWAY_NS} -o jsonpath='{.spec.clusterIP}')
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r29
          changed_when: false
          failed_when:
            - r29.rc != 0
            - "'AlreadyExists' not in (r29.stderr | default(''))"
            - "'already exists' not in (r29.stderr | default(''))"
            - "'NotFound' not in (r29.stderr | default(''))"
        - name: "29 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-29.sh" <<'QAENV'
            export KUADRANT_GATEWAY_NS=abc123-gateway
            export KUADRANT_GATEWAY_NAME=ingress-gateway
            export KUADRANT_DEVELOPER_NS=abc123-user1
            export KUADRANT_ZONE_ROOT_DOMAIN=example.com
            export KUADRANT_SYSTEM_NS=$(oc get kuadrant -A -o jsonpath="{.items[0].metadata.namespace}")
            export GATEWAY_ADDRESS=$(oc get svc ${KUADRANT_GATEWAY_NAME}-openshift-default -n ${KUADRANT_GATEWAY_NS} -o jsonpath='{.spec.clusterIP}')
            QAENV
            cat "{{ logdir }}/exports-29.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "29 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Module 03: Advanced Usage
              {{ r29.rc | default('?') }}
              {{ r29.stdout | default('') }}
              {{ r29.stderr | default('') }}
            dest: "{{ logdir }}/29.log"
    - block:
        - name: "30 | module-03-advanced.adoc | Exercise 1 Add rate-limit headers to an API response"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get limitador -A
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r30
          changed_when: false
          failed_when:
            - r30.rc != 0
            - "'AlreadyExists' not in (r30.stderr | default(''))"
            - "'already exists' not in (r30.stderr | default(''))"
            - "'NotFound' not in (r30.stderr | default(''))"
      always:
        - name: "30 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 1: Add rate-limit headers to an API response
              {{ r30.rc | default('?') }}
              {{ r30.stdout | default('') }}
              {{ r30.stderr | default('') }}
            dest: "{{ logdir }}/30.log"
    - block:
        - name: "31 | module-03-advanced.adoc | Exercise 1 Add rate-limit headers to an API response"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc patch limitador limitador -n ${KUADRANT_SYSTEM_NS} --type=merge -p '{"spec": {"rateLimitHeaders": "DRAFT_VERSION_03"}}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r31
          changed_when: false
          failed_when:
            - r31.rc != 0
            - "'AlreadyExists' not in (r31.stderr | default(''))"
            - "'already exists' not in (r31.stderr | default(''))"
            - "'NotFound' not in (r31.stderr | default(''))"
      always:
        - name: "31 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 1: Add rate-limit headers to an API response
              {{ r31.rc | default('?') }}
              {{ r31.stdout | default('') }}
              {{ r31.stderr | default('') }}
            dest: "{{ logdir }}/31.log"
    - block:
        - name: "32 | module-03-advanced.adoc | Exercise 1 Add rate-limit headers to an API response"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -i -k --resolve "api.${KUADRANT_ZONE_ROOT_DOMAIN}:443:${GATEWAY_ADDRESS}" -H 'Authorization: APIKEY IAMALICE' "https://api.${KUADRANT_ZONE_ROOT_DOMAIN}/cars"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r32
          changed_when: false
          failed_when:
            - r32.rc != 0
            - "'AlreadyExists' not in (r32.stderr | default(''))"
            - "'already exists' not in (r32.stderr | default(''))"
            - "'NotFound' not in (r32.stderr | default(''))"
      always:
        - name: "32 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 1: Add rate-limit headers to an API response
              {{ r32.rc | default('?') }}
              {{ r32.stdout | default('') }}
              {{ r32.stderr | default('') }}
            dest: "{{ logdir }}/32.log"
    - block:
        - name: "33 | module-03-advanced.adoc | Exercise 2 Token-based rate limiting for LLM inference APIs"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f - -n ${KUADRANT_GATEWAY_NS}
            apiVersion: kuadrant.io/v1alpha1
            kind: TokenRateLimitPolicy
            metadata:
              name: llm-protection
            spec:
              targetRef:
                group: gateway.networking.k8s.io
                kind: Gateway
                name: ${KUADRANT_GATEWAY_NAME}
              limits:
                "llm-users":
                  rates:
                    - limit: 10000
                      window: 24h
                  when:
                    - predicate: request.path == "/v1/chat/completions"
                  counters:
                    - expression: auth.identity.userid
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r33
          changed_when: false
          failed_when:
            - r33.rc != 0
            - "'AlreadyExists' not in (r33.stderr | default(''))"
            - "'already exists' not in (r33.stderr | default(''))"
            - "'NotFound' not in (r33.stderr | default(''))"
      always:
        - name: "33 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 2: Token-based rate limiting for LLM inference APIs
              {{ r33.rc | default('?') }}
              {{ r33.stdout | default('') }}
              {{ r33.stderr | default('') }}
            dest: "{{ logdir }}/33.log"
    - block:
        - name: "34 | module-03-advanced.adoc | Exercise 2 Token-based rate limiting for LLM inference APIs"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get tokenratelimitpolicy llm-protection -n ${KUADRANT_GATEWAY_NS} -o jsonpath='{.status.conditions}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r34
          changed_when: false
          failed_when:
            - r34.rc != 0
            - "'AlreadyExists' not in (r34.stderr | default(''))"
            - "'already exists' not in (r34.stderr | default(''))"
            - "'NotFound' not in (r34.stderr | default(''))"
      always:
        - name: "34 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 2: Token-based rate limiting for LLM inference APIs
              {{ r34.rc | default('?') }}
              {{ r34.stdout | default('') }}
              {{ r34.stderr | default('') }}
            dest: "{{ logdir }}/34.log"
    - block:
        - name: "35 | module-03-advanced.adoc | Exercise 4 Observe gateways policies and requests"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc patch kuadrant kuadrant -n ${KUADRANT_SYSTEM_NS} --type=merge -p '{"spec": {"observability": {"enable": true}}}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r35
          changed_when: false
          failed_when:
            - r35.rc != 0
            - "'AlreadyExists' not in (r35.stderr | default(''))"
            - "'already exists' not in (r35.stderr | default(''))"
            - "'NotFound' not in (r35.stderr | default(''))"
      always:
        - name: "35 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 4: Observe gateways, policies, and requests
              {{ r35.rc | default('?') }}
              {{ r35.stdout | default('') }}
              {{ r35.stderr | default('') }}
            dest: "{{ logdir }}/35.log"
    - block:
        - name: "36 | module-03-advanced.adoc | Exercise 4 Observe gateways policies and requests"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get servicemonitor,podmonitor -A -l kuadrant.io/observability=true
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r36
          changed_when: false
          failed_when:
            - r36.rc != 0
            - "'AlreadyExists' not in (r36.stderr | default(''))"
            - "'already exists' not in (r36.stderr | default(''))"
            - "'NotFound' not in (r36.stderr | default(''))"
      always:
        - name: "36 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 4: Observe gateways, policies, and requests
              {{ r36.rc | default('?') }}
              {{ r36.stdout | default('') }}
              {{ r36.stderr | default('') }}
            dest: "{{ logdir }}/36.log"
    - block:
        - name: "37 | module-03-advanced.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get servicemonitor,podmonitor -A -l kuadrant.io/observability=true
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r37
          changed_when: false
          failed_when:
            - r37.rc != 0
            - "'AlreadyExists' not in (r37.stderr | default(''))"
            - "'already exists' not in (r37.stderr | default(''))"
            - "'NotFound' not in (r37.stderr | default(''))"
      always:
        - name: "37 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Verify
              {{ r37.rc | default('?') }}
              {{ r37.stdout | default('') }}
              {{ r37.stderr | default('') }}
            dest: "{{ logdir }}/37.log"
