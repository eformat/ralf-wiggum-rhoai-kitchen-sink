# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: llmd-priority-flow-control (20 runnable blocks, 2 skipped)
- name: "Lab llmd-priority-flow-control"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/llmd-priority-flow-control/work"
    logdir: "{{ playbook_dir }}/../runs/llmd-priority-flow-control"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-getting-started.adoc | Exercise 1 Inspect your llm-d deployment and the Endpoint Pi"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get llminferenceservice -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Inspect your llm-d deployment and the Endpoint Picker
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-getting-started.adoc | Exercise 1 Inspect your llm-d deployment and the Endpoint Pi"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get pods -n abc123-user1 | grep router-scheduler
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Inspect your llm-d deployment and the Endpoint Picker
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-getting-started.adoc | Exercise 2 Enable flow control and saturation detection"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            LLM_SVC=$(oc get llminferenceservice -n abc123-user1 -o jsonpath='{.items[0].metadata.name}')
            echo "Configuring $LLM_SVC"
            oc patch llminferenceservice $LLM_SVC -n abc123-user1 --type=merge -p '{"spec":{"router":{"scheduler":{"config":{"inline":{"apiVersion":"llm-d.ai/v1alpha1","kind":"EndpointPickerConfig","featureGates":["flowControl"],"flowControl":{"saturationDetector":{"pluginRef":"utilization-detector"}},"plugins":[{"name":"utilization-detector","type":"utilization-detector","parameters":{"queueDepthThreshold":5,"kvCacheUtilThreshold":0.8,"metricsStalenessThreshold":"200ms"}}]}}}}}}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
        - name: "06 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-06.sh" <<'QAENV'
            LLM_SVC=$(oc get llminferenceservice -n abc123-user1 -o jsonpath='{.items[0].metadata.name}')
            QAENV
            cat "{{ logdir }}/exports-06.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 2: Enable flow control and saturation detection
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-getting-started.adoc | Exercise 2 Enable flow control and saturation detection"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            until oc get pods -n abc123-user1 2>/dev/null | grep router-scheduler | grep "1/1" >/dev/null; do sleep 10; echo "waiting for EPP router-scheduler..."; done
            oc get pods -n abc123-user1 | grep router-scheduler
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 2: Enable flow control and saturation detection
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get llminferenceservice $LLM_SVC -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get servicemonitors -n abc123-user1 -l app.kubernetes.io/component=llmmonitoring
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-02-hands-on.adoc | Exercise 1 Create priority tiers with InferenceObjective"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            NS=abc123-user1
            POOL=$(oc get inferencepools.inference.networking.k8s.io -n $NS -o jsonpath='{.items[0].metadata.name}')
            echo "Using InferencePool: $POOL"
            cat > critical-inference-objective.yaml <<EOF
            apiVersion: llm-d.ai/v1alpha2
            kind: InferenceObjective
            metadata:
              name: interactive-critical
              namespace: $NS
            spec:
              priority: 100
              poolRef:
                group: llm-d.ai
                kind: InferencePool
                name: $POOL
            EOF
            oc apply -f critical-inference-objective.yaml -n $NS
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
        - name: "10 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-10.sh" <<'QAENV'
            NS=abc123-user1
            POOL=$(oc get inferencepools.inference.networking.k8s.io -n $NS -o jsonpath='{.items[0].metadata.name}')
            QAENV
            cat "{{ logdir }}/exports-10.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Create priority tiers with InferenceObjective
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
    - block:
        - name: "11 | module-02-hands-on.adoc | Exercise 1 Create priority tiers with InferenceObjective"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > batch-inference-objective.yaml <<EOF
            apiVersion: llm-d.ai/v1alpha2
            kind: InferenceObjective
            metadata:
              name: batch-workload
              namespace: $NS
            spec:
              priority: -1
              poolRef:
                group: llm-d.ai
                kind: InferencePool
                name: $POOL
            EOF
            oc apply -f batch-inference-objective.yaml -n $NS
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r11
          changed_when: false
          failed_when:
            - r11.rc != 0
            - "'AlreadyExists' not in (r11.stderr | default(''))"
            - "'already exists' not in (r11.stderr | default(''))"
            - "'NotFound' not in (r11.stderr | default(''))"
      always:
        - name: "11 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Create priority tiers with InferenceObjective
              {{ r11.rc | default('?') }}
              {{ r11.stdout | default('') }}
              {{ r11.stderr | default('') }}
            dest: "{{ logdir }}/11.log"
    - block:
        - name: "12 | module-02-hands-on.adoc | Exercise 1 Create priority tiers with InferenceObjective"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc apply -f batch-inference-objective.yaml -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r12
          changed_when: false
          failed_when:
            - r12.rc != 0
            - "'AlreadyExists' not in (r12.stderr | default(''))"
            - "'already exists' not in (r12.stderr | default(''))"
            - "'NotFound' not in (r12.stderr | default(''))"
      always:
        - name: "12 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Create priority tiers with InferenceObjective
              {{ r12.rc | default('?') }}
              {{ r12.stdout | default('') }}
              {{ r12.stderr | default('') }}
            dest: "{{ logdir }}/12.log"
    - block:
        - name: "13 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get inferenceobjective -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r13
          changed_when: false
          failed_when:
            - r13.rc != 0
            - "'AlreadyExists' not in (r13.stderr | default(''))"
            - "'already exists' not in (r13.stderr | default(''))"
            - "'NotFound' not in (r13.stderr | default(''))"
      always:
        - name: "13 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r13.rc | default('?') }}
              {{ r13.stdout | default('') }}
              {{ r13.stderr | default('') }}
            dest: "{{ logdir }}/13.log"
    - block:
        - name: "14 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get inferenceobjective interactive-critical -n abc123-user1 -o jsonpath='{.spec.priority}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r14
          changed_when: false
          failed_when:
            - r14.rc != 0
            - "'AlreadyExists' not in (r14.stderr | default(''))"
            - "'already exists' not in (r14.stderr | default(''))"
            - "'NotFound' not in (r14.stderr | default(''))"
      always:
        - name: "14 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r14.rc | default('?') }}
              {{ r14.stdout | default('') }}
              {{ r14.stderr | default('') }}
            dest: "{{ logdir }}/14.log"
    - block:
        - name: "15 | module-02-hands-on.adoc | Exercise 2 Generate sustained load with an in-cluster load g"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            NS=abc123-user1
            LLM_SVC=$(oc get llminferenceservice -n $NS -o jsonpath='{.items[0].metadata.name}')
            GATEWAY_SVC=$(oc get svc -n $NS -l gateway.networking.k8s.io/gateway-name -o jsonpath='{.items[0].metadata.name}')
            URL="https://${GATEWAY_SVC}.${NS}.svc.cluster.local/${NS}/${LLM_SVC}/v1/chat/completions"
            MODEL_NAME=${MODEL_NAME:-RedHatAI/Qwen3-8B-FP8-dynamic}
            echo $URL
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r15
          changed_when: false
          failed_when:
            - r15.rc != 0
            - "'AlreadyExists' not in (r15.stderr | default(''))"
            - "'already exists' not in (r15.stderr | default(''))"
            - "'NotFound' not in (r15.stderr | default(''))"
        - name: "15 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-15.sh" <<'QAENV'
            NS=abc123-user1
            LLM_SVC=$(oc get llminferenceservice -n $NS -o jsonpath='{.items[0].metadata.name}')
            GATEWAY_SVC=$(oc get svc -n $NS -l gateway.networking.k8s.io/gateway-name -o jsonpath='{.items[0].metadata.name}')
            URL="https://${GATEWAY_SVC}.${NS}.svc.cluster.local/${NS}/${LLM_SVC}/v1/chat/completions"
            MODEL_NAME=${MODEL_NAME:-RedHatAI/Qwen3-8B-FP8-dynamic}
            QAENV
            cat "{{ logdir }}/exports-15.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "15 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Generate sustained load with an in-cluster load generator
              {{ r15.rc | default('?') }}
              {{ r15.stdout | default('') }}
              {{ r15.stderr | default('') }}
            dest: "{{ logdir }}/15.log"
    - block:
        - name: "16 | module-02-hands-on.adoc | Exercise 2 Generate sustained load with an in-cluster load g"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            read -r -d '' LOAD_SCRIPT << 'EOF' || true
            #!/bin/sh
            set -e
            URL="$1"; CONCURRENCY="$2"; REQUESTS="$3"; MODEL="$4"
            cat > /tmp/req.sh << 'REQEOF'
            #!/bin/sh
            if [ $(($2 % 2)) -eq 0 ]; then OBJ="batch-workload"; else OBJ="interactive-critical"; fi
            curl -sk --max-time 600 "$1" \
              -H "Content-Type: application/json" \
              -H "x-gateway-inference-objective: $OBJ" \
              -d "{\"model\":\"$4\",\"messages\":[{\"role\":\"user\",\"content\":\"Request $2. Write a detailed essay about topic $2 covering history, analysis, and predictions.\"}],\"max_tokens\":2048}" \
              -o /dev/null -w "req=$2 objective=$OBJ status=%{http_code} time=%{time_total}s\n"
            REQEOF
            chmod +x /tmp/req.sh
            echo "Smoke test..."
            STATUS=$(curl -sk --max-time 30 "$URL" \
              -H "Content-Type: application/json" \
              -H "x-gateway-inference-objective: interactive-critical" \
              -d "{\"model\":\"$MODEL\",\"messages\":[{\"role\":\"user\",\"content\":\"Hi\"}],\"max_tokens\":5}" \
              -o /dev/null -w "%{http_code}")
            echo "Status: $STATUS"
            if [ "$STATUS" != "200" ]; then
              echo "ERROR: Smoke test failed (HTTP $STATUS)"
              exit 1
            fi
            echo "Sending $REQUESTS requests ($CONCURRENCY concurrent)..."
            seq 1 "$REQUESTS" | xargs -P "$CONCURRENCY" -I{} /tmp/req.sh "$URL" {} 
            echo "Done."
            EOF
            oc create configmap load-test-script -n $NS --from-literal=load.sh="$LOAD_SCRIPT"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r16
          changed_when: false
          failed_when:
            - r16.rc != 0
            - "'AlreadyExists' not in (r16.stderr | default(''))"
            - "'already exists' not in (r16.stderr | default(''))"
            - "'NotFound' not in (r16.stderr | default(''))"
        - name: "16 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-16.sh" <<'QAENV'
            URL="$1"; CONCURRENCY="$2"; REQUESTS="$3"; MODEL="$4"
            STATUS=$(curl -sk --max-time 30 "$URL" -H "Content-Type: application/json" -H "x-gateway-inference-objective: interactive-critical" -d "{\"model\":\"$MODEL\",\"messages\":[{\"role\":\"user\",\"content\":\"Hi\"}],\"max_tokens\":5}" -o /dev/null -w "%{http_code}")
            QAENV
            cat "{{ logdir }}/exports-16.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "16 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Generate sustained load with an in-cluster load generator
              {{ r16.rc | default('?') }}
              {{ r16.stdout | default('') }}
              {{ r16.stderr | default('') }}
            dest: "{{ logdir }}/16.log"
    - block:
        - name: "17 | module-02-hands-on.adoc | Exercise 2 Generate sustained load with an in-cluster load g"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > load-test-pod.yaml <<EOF
            apiVersion: v1
            kind: Pod
            metadata:
              name: load-test
              namespace: $NS
            spec:
              restartPolicy: Never
              containers:
              - name: load
                image: curlimages/curl
                command: ["sh", "/scripts/load.sh"]
                args: ["$URL", "200", "5000", "$MODEL_NAME"]
                resources:
                  requests:
                    cpu: "4"
                    memory: "4Gi"
                  limits:
                    cpu: "8"
                    memory: "8Gi"
                volumeMounts:
                - name: script
                  mountPath: /scripts
              volumes:
              - name: script
                configMap:
                  name: load-test-script
                  defaultMode: 0755
            EOF
            oc apply -f load-test-pod.yaml -n abc123-user1
            oc wait --for=condition=Ready pod/load-test -n abc123-user1 --timeout=120s
            oc logs -f load-test -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r17
          changed_when: false
          failed_when:
            - r17.rc != 0
            - "'AlreadyExists' not in (r17.stderr | default(''))"
            - "'already exists' not in (r17.stderr | default(''))"
            - "'NotFound' not in (r17.stderr | default(''))"
      always:
        - name: "17 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Generate sustained load with an in-cluster load generator
              {{ r17.rc | default('?') }}
              {{ r17.stdout | default('') }}
              {{ r17.stderr | default('') }}
            dest: "{{ logdir }}/17.log"
    - block:
        - name: "18 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc logs load-test -n abc123-user1 | grep -o "status=[0-9]*" | sort | uniq -c
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r18
          changed_when: false
          failed_when:
            - r18.rc != 0
            - "'AlreadyExists' not in (r18.stderr | default(''))"
            - "'already exists' not in (r18.stderr | default(''))"
            - "'NotFound' not in (r18.stderr | default(''))"
      always:
        - name: "18 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r18.rc | default('?') }}
              {{ r18.stdout | default('') }}
              {{ r18.stderr | default('') }}
            dest: "{{ logdir }}/18.log"
    - block:
        - name: "19 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc delete pod load-test -n abc123-user1
            oc delete configmap load-test-script -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r19
          changed_when: false
          failed_when:
            - r19.rc != 0
            - "'AlreadyExists' not in (r19.stderr | default(''))"
            - "'already exists' not in (r19.stderr | default(''))"
            - "'NotFound' not in (r19.stderr | default(''))"
      always:
        - name: "19 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r19.rc | default('?') }}
              {{ r19.stdout | default('') }}
              {{ r19.stderr | default('') }}
            dest: "{{ logdir }}/19.log"
    - block:
        - name: "20 | module-02-hands-on.adoc | Exercise 3 Validate priority queuing and rejection behavior"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get pods -n abc123-user1 | grep vllm
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r20
          changed_when: false
          failed_when:
            - r20.rc != 0
            - "'AlreadyExists' not in (r20.stderr | default(''))"
            - "'already exists' not in (r20.stderr | default(''))"
            - "'NotFound' not in (r20.stderr | default(''))"
      always:
        - name: "20 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Validate priority queuing and rejection behavior under load
              {{ r20.rc | default('?') }}
              {{ r20.stdout | default('') }}
              {{ r20.stderr | default('') }}
            dest: "{{ logdir }}/20.log"
