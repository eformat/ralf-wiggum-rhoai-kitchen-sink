# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: maas-core (24 runnable blocks, 4 skipped)
- name: "Lab maas-core"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/maas-core/work"
    logdir: "{{ playbook_dir }}/../runs/maas-core"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-concepts.adoc | Exercise 1 Where the MaaS access model fits"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maassubscriptions -n models-as-a-service
            oc get maasauthpolicy -n models-as-a-service
            oc get maasmodelref --all-namespaces
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 1: Where the MaaS access model fits
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-concepts.adoc | Exercise 2 Inspect the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get crd | grep -E 'maas.opendatahub.io|aitenants'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the MaaS deployment
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-concepts.adoc | Exercise 2 Inspect the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get namespace openshift-user-workload-monitoring
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the MaaS deployment
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-concepts.adoc | Exercise 2 Inspect the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get tenants.maas.opendatahub.io -n models-as-a-service
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the MaaS deployment
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-01-concepts.adoc | Exercise 2 Inspect the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get tenants.maas.opendatahub.io default-tenant -n models-as-a-service -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the MaaS deployment
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-01-concepts.adoc | Exercise 2 Inspect the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get tenants.maas.opendatahub.io default-tenant -n models-as-a-service -o jsonpath='{.status.conditions[?(@.type=="Ready")].message}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the MaaS deployment
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-01-concepts.adoc | Exercise 2 Inspect the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get odhdashboardconfig odh-dashboard-config -n redhat-ods-applications -o yaml | grep -A 5 "dashboardConfig:"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the MaaS deployment
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
    - block:
        - name: "11 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -H "Authorization: Bearer $MAAS_API_KEY" "$MAAS_URL/maas-api/v1/models"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r11
          changed_when: false
          failed_when:
            - r11.rc != 0
            - "'AlreadyExists' not in (r11.stderr | default(''))"
            - "'already exists' not in (r11.stderr | default(''))"
            - "'NotFound' not in (r11.stderr | default(''))"
      always:
        - name: "11 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r11.rc | default('?') }}
              {{ r11.stdout | default('') }}
              {{ r11.stderr | default('') }}
            dest: "{{ logdir }}/11.log"
    - block:
        - name: "12 | module-02-hands-on.adoc | Exercise 3 Call a model through the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -X GET "$MAAS_URL/v1/models" -H "Authorization: Bearer $MAAS_API_KEY"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r12
          changed_when: false
          failed_when:
            - r12.rc != 0
            - "'AlreadyExists' not in (r12.stderr | default(''))"
            - "'already exists' not in (r12.stderr | default(''))"
            - "'NotFound' not in (r12.stderr | default(''))"
      always:
        - name: "12 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Call a model through the gateway
              {{ r12.rc | default('?') }}
              {{ r12.stdout | default('') }}
              {{ r12.stderr | default('') }}
            dest: "{{ logdir }}/12.log"
    - block:
        - name: "13 | module-02-hands-on.adoc | Exercise 3 Call a model through the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -X POST "$MAAS_URL/v1/chat/completions" \
              -H "Authorization: Bearer $MAAS_API_KEY" \
              -H "Content-Type: application/json" \
              -d "{\"model\":\"$MODEL_NAME\",\"messages\":[{\"role\":\"user\",\"content\":\"Explain quantum computing in simple terms.\"}],\"max_tokens\":150,\"temperature\":0.7}"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r13
          changed_when: false
          failed_when:
            - r13.rc != 0
            - "'AlreadyExists' not in (r13.stderr | default(''))"
            - "'already exists' not in (r13.stderr | default(''))"
            - "'NotFound' not in (r13.stderr | default(''))"
      always:
        - name: "13 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Call a model through the gateway
              {{ r13.rc | default('?') }}
              {{ r13.stdout | default('') }}
              {{ r13.stderr | default('') }}
            dest: "{{ logdir }}/13.log"
    - block:
        - name: "14 | module-02-hands-on.adoc | Exercise 3 Call a model through the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -X POST "$MAAS_URL/llm/$MODEL_NAME/v1/chat/completions" \
              -H "Authorization: Bearer $MAAS_API_KEY" \
              -H "Content-Type: application/json" \
              -d "{\"model\":\"$MODEL_NAME\",\"messages\":[{\"role\":\"user\",\"content\":\"Explain quantum computing in simple terms.\"}],\"max_tokens\":150}"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r14
          changed_when: false
          failed_when:
            - r14.rc != 0
            - "'AlreadyExists' not in (r14.stderr | default(''))"
            - "'already exists' not in (r14.stderr | default(''))"
            - "'NotFound' not in (r14.stderr | default(''))"
      always:
        - name: "14 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Call a model through the gateway
              {{ r14.rc | default('?') }}
              {{ r14.stdout | default('') }}
              {{ r14.stderr | default('') }}
            dest: "{{ logdir }}/14.log"
    - block:
        - name: "15 | module-02-hands-on.adoc | Exercise 4 See the token mechanism at work"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -o /dev/null -w "%{http_code}\n" \
              -H "Authorization: Bearer invalid-key" \
              "$MAAS_URL/maas-api/v1/models"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r15
          changed_when: false
          failed_when:
            - r15.rc != 0
            - "'AlreadyExists' not in (r15.stderr | default(''))"
            - "'already exists' not in (r15.stderr | default(''))"
            - "'NotFound' not in (r15.stderr | default(''))"
      always:
        - name: "15 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: See the token mechanism at work
              {{ r15.rc | default('?') }}
              {{ r15.stdout | default('') }}
              {{ r15.stderr | default('') }}
            dest: "{{ logdir }}/15.log"
    - block:
        - name: "16 | module-02-hands-on.adoc | Exercise 4 See the token mechanism at work"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            for i in $(seq 1 20); do
              curl -s -o /dev/null -w "%{http_code}\n" \
                -X POST "$MAAS_URL/v1/chat/completions" \
                -H "Authorization: Bearer $MAAS_API_KEY" \
                -H "Content-Type: application/json" \
                -d "{\"model\":\"$MODEL_NAME\",\"messages\":[{\"role\":\"user\",\"content\":\"ping\"}],\"max_tokens\":50}"
            done | sort | uniq -c
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r16
          changed_when: false
          failed_when:
            - r16.rc != 0
            - "'AlreadyExists' not in (r16.stderr | default(''))"
            - "'already exists' not in (r16.stderr | default(''))"
            - "'NotFound' not in (r16.stderr | default(''))"
      always:
        - name: "16 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: See the token mechanism at work
              {{ r16.rc | default('?') }}
              {{ r16.stdout | default('') }}
              {{ r16.stderr | default('') }}
            dest: "{{ logdir }}/16.log"
    - block:
        - name: "17 | module-03-advanced.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maassubscription -n models-as-a-service
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r17
          changed_when: false
          failed_when:
            - r17.rc != 0
            - "'AlreadyExists' not in (r17.stderr | default(''))"
            - "'already exists' not in (r17.stderr | default(''))"
            - "'NotFound' not in (r17.stderr | default(''))"
      always:
        - name: "17 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Verify
              {{ r17.rc | default('?') }}
              {{ r17.stdout | default('') }}
              {{ r17.stderr | default('') }}
            dest: "{{ logdir }}/17.log"
    - block:
        - name: "18 | module-03-advanced.adoc | Exercise 2 Apply governance by YAML"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > workshop-quota-yaml.yaml <<'EOF'
            apiVersion: maas.opendatahub.io/v1alpha1
            kind: MaaSSubscription
            metadata:
              name: workshop-quota-yaml
              namespace: models-as-a-service <1>
            spec:
              owner:
                groups:
                  - name: system:authenticated:oauth <2>
                    users: []
              modelRefs:
                - name: granite-vllm-maas <3>
                  namespace: abc123-user1 <4>
              tokenRateLimits: <5>
                - limit: 100
                  window: 1m
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r18
          changed_when: false
          failed_when:
            - r18.rc != 0
            - "'AlreadyExists' not in (r18.stderr | default(''))"
            - "'already exists' not in (r18.stderr | default(''))"
            - "'NotFound' not in (r18.stderr | default(''))"
      always:
        - name: "18 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 2: Apply governance by YAML
              {{ r18.rc | default('?') }}
              {{ r18.stdout | default('') }}
              {{ r18.stderr | default('') }}
            dest: "{{ logdir }}/18.log"
    - block:
        - name: "19 | module-03-advanced.adoc | Exercise 2 Apply governance by YAML"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > workshop-access-yaml.yaml <<'EOF'
            apiVersion: maas.opendatahub.io/v1alpha1
            kind: MaaSAuthPolicy
            metadata:
              name: workshop-access-yaml
              namespace: models-as-a-service
            spec:
              modelRefs:
                - name: granite-vllm-maas
                  namespace: abc123-user1 <1>
              subjects:
                groups:
                  - name: system:authenticated:oauth <2>
                    users: []
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r19
          changed_when: false
          failed_when:
            - r19.rc != 0
            - "'AlreadyExists' not in (r19.stderr | default(''))"
            - "'already exists' not in (r19.stderr | default(''))"
            - "'NotFound' not in (r19.stderr | default(''))"
      always:
        - name: "19 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 2: Apply governance by YAML
              {{ r19.rc | default('?') }}
              {{ r19.stdout | default('') }}
              {{ r19.stderr | default('') }}
            dest: "{{ logdir }}/19.log"
    - block:
        - name: "20 | module-03-advanced.adoc | Exercise 2 Apply governance by YAML"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc apply -f workshop-quota-yaml.yaml
            oc apply -f workshop-access-yaml.yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r20
          changed_when: false
          failed_when:
            - r20.rc != 0
            - "'AlreadyExists' not in (r20.stderr | default(''))"
            - "'already exists' not in (r20.stderr | default(''))"
            - "'NotFound' not in (r20.stderr | default(''))"
      always:
        - name: "20 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 2: Apply governance by YAML
              {{ r20.rc | default('?') }}
              {{ r20.stdout | default('') }}
              {{ r20.stderr | default('') }}
            dest: "{{ logdir }}/20.log"
    - block:
        - name: "21 | module-03-advanced.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maassubscription workshop-quota-yaml -n models-as-a-service
            oc get maasauthpolicy workshop-access-yaml -n models-as-a-service
            oc get authpolicy -n abc123-user1
            oc get tokenratelimitpolicy -n abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r21
          changed_when: false
          failed_when:
            - r21.rc != 0
            - "'AlreadyExists' not in (r21.stderr | default(''))"
            - "'already exists' not in (r21.stderr | default(''))"
            - "'NotFound' not in (r21.stderr | default(''))"
      always:
        - name: "21 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Verify
              {{ r21.rc | default('?') }}
              {{ r21.stdout | default('') }}
              {{ r21.stderr | default('') }}
            dest: "{{ logdir }}/21.log"
    - block:
        - name: "22 | module-03-advanced.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc logs -n redhat-ai-gateway-infra -l app=maas-controller --tail=50
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r22
          changed_when: false
          failed_when:
            - r22.rc != 0
            - "'AlreadyExists' not in (r22.stderr | default(''))"
            - "'already exists' not in (r22.stderr | default(''))"
            - "'NotFound' not in (r22.stderr | default(''))"
      always:
        - name: "22 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Verify
              {{ r22.rc | default('?') }}
              {{ r22.stdout | default('') }}
              {{ r22.stderr | default('') }}
            dest: "{{ logdir }}/22.log"
    - block:
        - name: "23 | module-03-advanced.adoc | Exercise 3 Manage keys and monitor usage"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc patch tenants.maas.opendatahub.io default-tenant -n models-as-a-service \
              --type merge \
              -p '{"spec":{"apiKeys":{"maxExpirationDays":90}}}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r23
          changed_when: false
          failed_when:
            - r23.rc != 0
            - "'AlreadyExists' not in (r23.stderr | default(''))"
            - "'already exists' not in (r23.stderr | default(''))"
            - "'NotFound' not in (r23.stderr | default(''))"
      always:
        - name: "23 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 3: Manage keys and monitor usage
              {{ r23.rc | default('?') }}
              {{ r23.stdout | default('') }}
              {{ r23.stderr | default('') }}
            dest: "{{ logdir }}/23.log"
    - block:
        - name: "24 | module-03-advanced.adoc | Exercise 3 Manage keys and monitor usage"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get tenants.maas.opendatahub.io default-tenant -n models-as-a-service -o jsonpath='{.spec.apiKeys.maxExpirationDays}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r24
          changed_when: false
          failed_when:
            - r24.rc != 0
            - "'AlreadyExists' not in (r24.stderr | default(''))"
            - "'already exists' not in (r24.stderr | default(''))"
            - "'NotFound' not in (r24.stderr | default(''))"
      always:
        - name: "24 | save log"
          ansible.builtin.copy:
            content: |
              # module-03-advanced.adoc — Exercise 3: Manage keys and monitor usage
              {{ r24.rc | default('?') }}
              {{ r24.stdout | default('') }}
              {{ r24.stderr | default('') }}
            dest: "{{ logdir }}/24.log"
