# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: maas-multi-tenancy (24 runnable blocks, 16 skipped)
- name: "Lab maas-multi-tenancy"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/maas-multi-tenancy/work"
    logdir: "{{ playbook_dir }}/../runs/maas-multi-tenancy"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-concepts.adoc | Exercise 2 Inspect the cluster"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get aitenant models-as-a-service -n ai-tenants
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the cluster
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-concepts.adoc | Exercise 2 Inspect the cluster"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gatewayclass openshift-default
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the cluster
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-concepts.adoc | Exercise 2 Inspect the cluster"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get aitenant -n ai-tenants
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the cluster
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-concepts.adoc | Exercise 2 Inspect the cluster"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maastenantconfig default-tenant -n models-as-a-service
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-concepts.adoc — Exercise 2: Inspect the cluster
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export TENANT_NAME=red-team
            export CLUSTER_DOMAIN=$(oc get ingresses.config.openshift.io cluster -o jsonpath={.spec.domain})
            export GATEWAY_HOSTNAME="${TENANT_NAME}-maas.${CLUSTER_DOMAIN}"
            export GATEWAY_NAMESPACE="openshift-ingress"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
        - name: "08 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-08.sh" <<'QAENV'
            export TENANT_NAME=red-team
            export CLUSTER_DOMAIN=$(oc get ingresses.config.openshift.io cluster -o jsonpath={.spec.domain})
            export GATEWAY_HOSTNAME="${TENANT_NAME}-maas.${CLUSTER_DOMAIN}"
            export GATEWAY_NAMESPACE="openshift-ingress"
            QAENV
            cat "{{ logdir }}/exports-08.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc label namespace redhat-ai-gateway-infra \
              "maas.opendatahub.io/gateway-access-${TENANT_NAME}=true" --overwrite
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > tenant-gateway.yaml <<EOF
            apiVersion: v1
            kind: ConfigMap
            metadata:
              name: ${TENANT_NAME}-config
              namespace: ${GATEWAY_NAMESPACE}
            data:
              service: |
                metadata:
                  annotations:
                    service.beta.openshift.io/serving-cert-secret-name: "${TENANT_NAME}-gw-service-tls"
                spec:
                  type: LoadBalancer
            ---
            apiVersion: gateway.networking.k8s.io/v1
            kind: Gateway
            metadata:
              name: ${TENANT_NAME}
              namespace: ${GATEWAY_NAMESPACE}
              annotations:
                opendatahub.io/managed: "false"
                security.opendatahub.io/authorino-tls-bootstrap: "true"
            spec:
              # data-science-gateway-class works on clusters without a LoadBalancer
              # provider (RHDP workshop clusters, bare-metal) — the same class the
              # maas-default-gateway uses. On cloud clusters with a LoadBalancer provider,
              # use gatewayClassName: openshift-default.
              gatewayClassName: data-science-gateway-class
              infrastructure:
                parametersRef:
                  group: ""
                  kind: ConfigMap
                  name: ${TENANT_NAME}-config
              listeners:
              - name: https
                hostname: "${GATEWAY_HOSTNAME}"
                port: 443
                protocol: HTTPS
                allowedRoutes:
                  namespaces:
                    from: Selector
                    selector:
                      matchLabels:
                        maas.opendatahub.io/gateway-access-${TENANT_NAME}: "true"
                tls:
                  mode: Terminate
                  certificateRefs:
                  - group: ""
                    kind: Secret
                    name: ${TENANT_NAME}-gw-service-tls
            EOF
            oc apply -f tenant-gateway.yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
    - block:
        - name: "11 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > tenant-gateway-route.yaml <<EOF
            apiVersion: route.openshift.io/v1
            kind: Route
            metadata:
              name: ${TENANT_NAME}-route
              namespace: ${GATEWAY_NAMESPACE}
              labels:
                gateway.networking.k8s.io/gateway-name: ${TENANT_NAME}
            spec:
              host: "${GATEWAY_HOSTNAME}"
              to:
                kind: Service
                name: ${TENANT_NAME}-data-science-gateway-class
                weight: 100
              port:
                targetPort: https
              tls:
                termination: reencrypt
                insecureEdgeTerminationPolicy: Redirect
            EOF
            oc apply -f tenant-gateway-route.yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r11
          changed_when: false
          failed_when:
            - r11.rc != 0
            - "'AlreadyExists' not in (r11.stderr | default(''))"
            - "'already exists' not in (r11.stderr | default(''))"
            - "'NotFound' not in (r11.stderr | default(''))"
      always:
        - name: "11 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r11.rc | default('?') }}
              {{ r11.stdout | default('') }}
              {{ r11.stderr | default('') }}
            dest: "{{ logdir }}/11.log"
    - block:
        - name: "12 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc patch service ${TENANT_NAME}-data-science-gateway-class -n ${GATEWAY_NAMESPACE} \
              --subresource=status --type merge \
              -p "{\"status\":{\"loadBalancer\":{\"ingress\":[{\"hostname\":\"${GATEWAY_HOSTNAME}\"}]}}}"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r12
          changed_when: false
          failed_when:
            - r12.rc != 0
            - "'AlreadyExists' not in (r12.stderr | default(''))"
            - "'already exists' not in (r12.stderr | default(''))"
            - "'NotFound' not in (r12.stderr | default(''))"
      always:
        - name: "12 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r12.rc | default('?') }}
              {{ r12.stdout | default('') }}
              {{ r12.stderr | default('') }}
            dest: "{{ logdir }}/12.log"
    - block:
        - name: "13 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc apply -f tenant-gateway.yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r13
          changed_when: false
          failed_when:
            - r13.rc != 0
            - "'AlreadyExists' not in (r13.stderr | default(''))"
            - "'already exists' not in (r13.stderr | default(''))"
            - "'NotFound' not in (r13.stderr | default(''))"
      always:
        - name: "13 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r13.rc | default('?') }}
              {{ r13.stdout | default('') }}
              {{ r13.stderr | default('') }}
            dest: "{{ logdir }}/13.log"
    - block:
        - name: "14 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gateway ${TENANT_NAME} -n ${GATEWAY_NAMESPACE}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r14
          changed_when: false
          failed_when:
            - r14.rc != 0
            - "'AlreadyExists' not in (r14.stderr | default(''))"
            - "'already exists' not in (r14.stderr | default(''))"
            - "'NotFound' not in (r14.stderr | default(''))"
      always:
        - name: "14 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r14.rc | default('?') }}
              {{ r14.stdout | default('') }}
              {{ r14.stderr | default('') }}
            dest: "{{ logdir }}/14.log"
    - block:
        - name: "15 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat > tenant-aitenant.yaml <<EOF
            apiVersion: maas.opendatahub.io/v1alpha1
            kind: AITenant
            metadata:
              name: ${TENANT_NAME}
              namespace: ai-tenants
            spec:
              gateway:
                name: ${TENANT_NAME}
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r15
          changed_when: false
          failed_when:
            - r15.rc != 0
            - "'AlreadyExists' not in (r15.stderr | default(''))"
            - "'already exists' not in (r15.stderr | default(''))"
            - "'NotFound' not in (r15.stderr | default(''))"
      always:
        - name: "15 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r15.rc | default('?') }}
              {{ r15.stdout | default('') }}
              {{ r15.stderr | default('') }}
            dest: "{{ logdir }}/15.log"
    - block:
        - name: "16 | module-02-hands-on.adoc | Exercise 1 Provision a dedicated Gateway and AITenant"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc apply -f tenant-aitenant.yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r16
          changed_when: false
          failed_when:
            - r16.rc != 0
            - "'AlreadyExists' not in (r16.stderr | default(''))"
            - "'already exists' not in (r16.stderr | default(''))"
            - "'NotFound' not in (r16.stderr | default(''))"
      always:
        - name: "16 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Provision a dedicated Gateway and AITenant
              {{ r16.rc | default('?') }}
              {{ r16.stdout | default('') }}
              {{ r16.stderr | default('') }}
            dest: "{{ logdir }}/16.log"
    - block:
        - name: "17 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get namespace ai-tenant-${TENANT_NAME} --show-labels
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r17
          changed_when: false
          failed_when:
            - r17.rc != 0
            - "'AlreadyExists' not in (r17.stderr | default(''))"
            - "'already exists' not in (r17.stderr | default(''))"
            - "'NotFound' not in (r17.stderr | default(''))"
      always:
        - name: "17 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r17.rc | default('?') }}
              {{ r17.stdout | default('') }}
              {{ r17.stderr | default('') }}
            dest: "{{ logdir }}/17.log"
    - block:
        - name: "18 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maastenantconfig default-tenant -n ai-tenant-${TENANT_NAME}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r18
          changed_when: false
          failed_when:
            - r18.rc != 0
            - "'AlreadyExists' not in (r18.stderr | default(''))"
            - "'already exists' not in (r18.stderr | default(''))"
            - "'NotFound' not in (r18.stderr | default(''))"
      always:
        - name: "18 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r18.rc | default('?') }}
              {{ r18.stdout | default('') }}
              {{ r18.stderr | default('') }}
            dest: "{{ logdir }}/18.log"
    - block:
        - name: "19 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get deployment maas-api-${TENANT_NAME} -n redhat-ai-gateway-infra
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r19
          changed_when: false
          failed_when:
            - r19.rc != 0
            - "'AlreadyExists' not in (r19.stderr | default(''))"
            - "'already exists' not in (r19.stderr | default(''))"
            - "'NotFound' not in (r19.stderr | default(''))"
      always:
        - name: "19 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r19.rc | default('?') }}
              {{ r19.stdout | default('') }}
              {{ r19.stderr | default('') }}
            dest: "{{ logdir }}/19.log"
    - block:
        - name: "20 | module-02-hands-on.adoc | Exercise 2 Verify tenant isolation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get aitenant ${TENANT_NAME} -n ai-tenants
            oc get aitenant ${TENANT_NAME} -n ai-tenants -o jsonpath='{.status.conditions}' | jq .
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r20
          changed_when: false
          failed_when:
            - r20.rc != 0
            - "'AlreadyExists' not in (r20.stderr | default(''))"
            - "'already exists' not in (r20.stderr | default(''))"
            - "'NotFound' not in (r20.stderr | default(''))"
      always:
        - name: "20 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Verify tenant isolation
              {{ r20.rc | default('?') }}
              {{ r20.stdout | default('') }}
              {{ r20.stderr | default('') }}
            dest: "{{ logdir }}/20.log"
    - block:
        - name: "21 | module-02-hands-on.adoc | Exercise 2 Verify tenant isolation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export GATEWAY_NAME=$(oc get aitenant ${TENANT_NAME} -n ai-tenants \
              -o jsonpath='{.spec.gateway.name}')
            export GATEWAY_HOST=$(oc get gateway ${GATEWAY_NAME} -n openshift-ingress \
              -o jsonpath='{.spec.listeners[0].hostname}')
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r21
          changed_when: false
          failed_when:
            - r21.rc != 0
            - "'AlreadyExists' not in (r21.stderr | default(''))"
            - "'already exists' not in (r21.stderr | default(''))"
            - "'NotFound' not in (r21.stderr | default(''))"
        - name: "21 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-21.sh" <<'QAENV'
            export GATEWAY_NAME=$(oc get aitenant ${TENANT_NAME} -n ai-tenants -o jsonpath='{.spec.gateway.name}')
            export GATEWAY_HOST=$(oc get gateway ${GATEWAY_NAME} -n openshift-ingress -o jsonpath='{.spec.listeners[0].hostname}')
            QAENV
            cat "{{ logdir }}/exports-21.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "21 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Verify tenant isolation
              {{ r21.rc | default('?') }}
              {{ r21.stdout | default('') }}
              {{ r21.stderr | default('') }}
            dest: "{{ logdir }}/21.log"
    - block:
        - name: "22 | module-02-hands-on.adoc | Exercise 2 Verify tenant isolation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export GATEWAY_HOST=$(oc get route -n openshift-ingress \
              -l gateway.networking.k8s.io/gateway-name=${GATEWAY_NAME} \
              -o jsonpath='{.items[0].spec.host}')
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r22
          changed_when: false
          failed_when:
            - r22.rc != 0
            - "'AlreadyExists' not in (r22.stderr | default(''))"
            - "'already exists' not in (r22.stderr | default(''))"
            - "'NotFound' not in (r22.stderr | default(''))"
        - name: "22 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-22.sh" <<'QAENV'
            export GATEWAY_HOST=$(oc get route -n openshift-ingress -l gateway.networking.k8s.io/gateway-name=${GATEWAY_NAME} -o jsonpath='{.items[0].spec.host}')
            QAENV
            cat "{{ logdir }}/exports-22.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "22 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Verify tenant isolation
              {{ r22.rc | default('?') }}
              {{ r22.stdout | default('') }}
              {{ r22.stderr | default('') }}
            dest: "{{ logdir }}/22.log"
    - block:
        - name: "23 | module-02-hands-on.adoc | Exercise 2 Verify tenant isolation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export TOKEN=$(oc whoami -t)
            curl -sSk "https://${GATEWAY_HOST}/v1/models" \
              -H "Authorization: Bearer ${TOKEN}" \
              -H "Content-Type: application/json" | jq .
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r23
          changed_when: false
          failed_when:
            - r23.rc != 0
            - "'AlreadyExists' not in (r23.stderr | default(''))"
            - "'already exists' not in (r23.stderr | default(''))"
            - "'NotFound' not in (r23.stderr | default(''))"
        - name: "23 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-23.sh" <<'QAENV'
            export TOKEN=$(oc whoami -t)
            QAENV
            cat "{{ logdir }}/exports-23.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "23 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Verify tenant isolation
              {{ r23.rc | default('?') }}
              {{ r23.stdout | default('') }}
              {{ r23.stderr | default('') }}
            dest: "{{ logdir }}/23.log"
    - block:
        - name: "24 | module-02-hands-on.adoc | Exercise 2 Verify tenant isolation"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -sSk -o /dev/null -w "%{http_code}\n" \
              "https://${GATEWAY_HOST}/v1/models"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r24
          changed_when: false
          failed_when:
            - r24.rc != 0
            - "'AlreadyExists' not in (r24.stderr | default(''))"
            - "'already exists' not in (r24.stderr | default(''))"
            - "'NotFound' not in (r24.stderr | default(''))"
      always:
        - name: "24 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Verify tenant isolation
              {{ r24.rc | default('?') }}
              {{ r24.stdout | default('') }}
              {{ r24.stderr | default('') }}
            dest: "{{ logdir }}/24.log"
