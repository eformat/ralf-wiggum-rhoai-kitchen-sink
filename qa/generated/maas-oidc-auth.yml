# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: maas-oidc-auth (10 runnable blocks, 3 skipped)
- name: "Lab maas-oidc-auth"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/maas-oidc-auth/work"
    logdir: "{{ playbook_dir }}/../runs/maas-oidc-auth"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-getting-started.adoc | Exercise 1 Verify the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get aitenant models-as-a-service -n ai-tenants
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the MaaS deployment
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-getting-started.adoc | Exercise 1 Verify the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maastenantconfig default-tenant -n models-as-a-service \
              -o jsonpath='{.status.infraNamespace}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the MaaS deployment
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-getting-started.adoc | Exercise 1 Verify the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export MAAS_GATEWAY_URL=https://$(oc get route -n openshift-ingress \
              -l gateway.networking.k8s.io/gateway-name=maas-default-gateway \
              -o jsonpath='{.items[0].spec.host}')
            echo $MAAS_GATEWAY_URL
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
        - name: "06 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-06.sh" <<'QAENV'
            export MAAS_GATEWAY_URL=https://$(oc get route -n openshift-ingress -l gateway.networking.k8s.io/gateway-name=maas-default-gateway -o jsonpath='{.items[0].spec.host}')
            QAENV
            cat "{{ logdir }}/exports-06.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the MaaS deployment
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-getting-started.adoc | Exercise 1 Verify the MaaS deployment"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gateway maas-default-gateway -n openshift-ingress \
              -o jsonpath={.spec.listeners[0].hostname}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the MaaS deployment
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get aitenants.maas.opendatahub.io models-as-a-service -n ai-tenants \
              -o jsonpath='{.spec.oidc}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get maassubscription -n models-as-a-service -o yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-02-hands-on.adoc | Exercise 2 Verify external OIDC authentication"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -sSk -o /dev/null -w "%{http_code}\n" \
              "$MAAS_GATEWAY_URL/maas-api/v1/models"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Verify external OIDC authentication
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
