# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: mcp-gateway-operator (31 runnable blocks, 1 skipped)
- name: "Lab mcp-gateway-operator"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/mcp-gateway-operator/work"
    logdir: "{{ playbook_dir }}/../runs/mcp-gateway-operator"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-getting-started.adoc | Exercise 1 Install the MCP gateway Operator with OLM"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export MCP_SYSTEM=abc123-mcp-system
            export GATEWAY_NS=abc123-gateway
            export GATEWAY_NAME=mcp-gateway
            export MCP_LISTENER_HOST=mcp.example.com
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
        - name: "04 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-04.sh" <<'QAENV'
            export MCP_SYSTEM=abc123-mcp-system
            export GATEWAY_NS=abc123-gateway
            export GATEWAY_NAME=mcp-gateway
            export MCP_LISTENER_HOST=mcp.example.com
            QAENV
            cat "{{ logdir }}/exports-04.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Install the MCP gateway Operator with OLM
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-getting-started.adoc | Exercise 1 Install the MCP gateway Operator with OLM"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project ${MCP_SYSTEM}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Install the MCP gateway Operator with OLM
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-getting-started.adoc | Exercise 1 Install the MCP gateway Operator with OLM"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -n ${MCP_SYSTEM} -f -
            apiVersion: operators.coreos.com/v1alpha1
            kind: Subscription
            metadata:
              name: mcp-gateway
            spec:
              source: redhat-operators
              sourceNamespace: openshift-marketplace
              name: mcp-gateway
              channel: preview
            ---
            apiVersion: operators.coreos.com/v1
            kind: OperatorGroup
            metadata:
              name: mcp-gateway
            spec:
              targetNamespaces:
              - ${MCP_SYSTEM}
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Install the MCP gateway Operator with OLM
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-getting-started.adoc | Exercise 1 Install the MCP gateway Operator with OLM"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc wait --for=jsonpath={.status.installPlanRef.name} subscription mcp-gateway -n ${MCP_SYSTEM} --timeout=10s
            plan=$(oc get subscription mcp-gateway -n ${MCP_SYSTEM} -o=jsonpath={.status.installPlanRef.name})
            oc wait --for=condition=Installed installplan -n ${MCP_SYSTEM} ${plan} --timeout=60s
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Install the MCP gateway Operator with OLM
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc wait csv -n ${MCP_SYSTEM} -l operators.coreos.com/mcp-gateway.${MCP_SYSTEM}="" --for=jsonpath='{.status.phase}'=Succeeded --timeout=5m
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-01-getting-started.adoc | Exercise 2 Create a Gateway object with an MCP listener"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project ${GATEWAY_NS}
            cat <<EOF | oc apply -f -
            apiVersion: gateway.networking.k8s.io/v1
            kind: Gateway
            metadata:
              name: ${GATEWAY_NAME}
              namespace: ${GATEWAY_NS}
            spec:
              gatewayClassName: openshift-default
              listeners:
              - name: http
                port: 80
                protocol: HTTP
                allowedRoutes:
                  namespaces:
                    from: All
              - name: mcp
                hostname: ${MCP_LISTENER_HOST}
                port: 8080
                protocol: HTTP
                allowedRoutes:
                  namespaces:
                    from: All
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 2: Create a Gateway object with an MCP listener
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-01-getting-started.adoc | Exercise 2 Create a Gateway object with an MCP listener"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gateway ${GATEWAY_NAME} -n ${GATEWAY_NS} -o jsonpath='{.status.conditions[?(@.type=="Accepted")].status}{" "}{.status.conditions[?(@.type=="Programmed")].status}{"\n"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 2: Create a Gateway object with an MCP listener
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
    - block:
        - name: "11 | module-01-getting-started.adoc | Exercise 3 Connect the gateway with an MCPGatewayExtension C"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: gateway.networking.k8s.io/v1beta1
            kind: ReferenceGrant
            metadata:
              name: allow-mcp-extension
              namespace: ${GATEWAY_NS}
            spec:
              from:
              - group: mcp.kuadrant.io
                kind: MCPGatewayExtension
                namespace: ${MCP_SYSTEM}
              to:
              - group: gateway.networking.k8s.io
                kind: Gateway
              # the extension also reads the gateway config Secret cross-namespace
              - group: ""
                kind: Secret
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r11
          changed_when: false
          failed_when:
            - r11.rc != 0
            - "'AlreadyExists' not in (r11.stderr | default(''))"
            - "'already exists' not in (r11.stderr | default(''))"
            - "'NotFound' not in (r11.stderr | default(''))"
      always:
        - name: "11 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 3: Connect the gateway with an MCPGatewayExtension CR
              {{ r11.rc | default('?') }}
              {{ r11.stdout | default('') }}
              {{ r11.stderr | default('') }}
            dest: "{{ logdir }}/11.log"
    - block:
        - name: "12 | module-01-getting-started.adoc | Exercise 3 Connect the gateway with an MCPGatewayExtension C"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: mcp.kuadrant.io/v1alpha1
            kind: MCPGatewayExtension
            metadata:
              name: mcp-gateway-one
              namespace: ${MCP_SYSTEM}
            spec:
              targetRef:
                group: gateway.networking.k8s.io
                kind: Gateway
                name: ${GATEWAY_NAME}
                namespace: ${GATEWAY_NS}
                sectionName: mcp
              httpRouteManagement: Enabled
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r12
          changed_when: false
          failed_when:
            - r12.rc != 0
            - "'AlreadyExists' not in (r12.stderr | default(''))"
            - "'already exists' not in (r12.stderr | default(''))"
            - "'NotFound' not in (r12.stderr | default(''))"
      always:
        - name: "12 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 3: Connect the gateway with an MCPGatewayExtension CR
              {{ r12.rc | default('?') }}
              {{ r12.stdout | default('') }}
              {{ r12.stderr | default('') }}
            dest: "{{ logdir }}/12.log"
    - block:
        - name: "13 | module-01-getting-started.adoc | Exercise 3 Connect the gateway with an MCPGatewayExtension C"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc wait --for=condition=Ready mcpgatewayextension/mcp-gateway-one -n ${MCP_SYSTEM}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r13
          changed_when: false
          failed_when:
            - r13.rc != 0
            - "'AlreadyExists' not in (r13.stderr | default(''))"
            - "'already exists' not in (r13.stderr | default(''))"
            - "'NotFound' not in (r13.stderr | default(''))"
      always:
        - name: "13 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 3: Connect the gateway with an MCPGatewayExtension CR
              {{ r13.rc | default('?') }}
              {{ r13.stdout | default('') }}
              {{ r13.stderr | default('') }}
            dest: "{{ logdir }}/13.log"
    - block:
        - name: "14 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get httproute mcp-gateway-route -n ${MCP_SYSTEM}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r14
          changed_when: false
          failed_when:
            - r14.rc != 0
            - "'AlreadyExists' not in (r14.stderr | default(''))"
            - "'already exists' not in (r14.stderr | default(''))"
            - "'NotFound' not in (r14.stderr | default(''))"
      always:
        - name: "14 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r14.rc | default('?') }}
              {{ r14.stdout | default('') }}
              {{ r14.stderr | default('') }}
            dest: "{{ logdir }}/14.log"
    - block:
        - name: "15 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get envoyfilter -n ${GATEWAY_NS} -l app.kubernetes.io/managed-by=mcpgateway-controller
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r15
          changed_when: false
          failed_when:
            - r15.rc != 0
            - "'AlreadyExists' not in (r15.stderr | default(''))"
            - "'already exists' not in (r15.stderr | default(''))"
            - "'NotFound' not in (r15.stderr | default(''))"
      always:
        - name: "15 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r15.rc | default('?') }}
              {{ r15.stdout | default('') }}
              {{ r15.stderr | default('') }}
            dest: "{{ logdir }}/15.log"
    - block:
        - name: "16 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "Content-Type: application/json" \
              -H "Accept: application/json, text/event-stream" \
              -d '{"jsonrpc": "2.0", "id": 1, "method": "initialize"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r16
          changed_when: false
          failed_when:
            - r16.rc != 0
            - "'AlreadyExists' not in (r16.stderr | default(''))"
            - "'already exists' not in (r16.stderr | default(''))"
            - "'NotFound' not in (r16.stderr | default(''))"
      always:
        - name: "16 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r16.rc | default('?') }}
              {{ r16.stdout | default('') }}
              {{ r16.stderr | default('') }}
            dest: "{{ logdir }}/16.log"
    - block:
        - name: "17 | module-02-hands-on.adoc | Exercise 1 Register an MCP server with the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            export MCP_SYSTEM=abc123-mcp-system
            export GATEWAY_NS=abc123-gateway
            export GATEWAY_NAME=mcp-gateway
            export MCP_LISTENER_HOST=mcp.example.com
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r17
          changed_when: false
          failed_when:
            - r17.rc != 0
            - "'AlreadyExists' not in (r17.stderr | default(''))"
            - "'already exists' not in (r17.stderr | default(''))"
            - "'NotFound' not in (r17.stderr | default(''))"
        - name: "17 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-17.sh" <<'QAENV'
            export MCP_SYSTEM=abc123-mcp-system
            export GATEWAY_NS=abc123-gateway
            export GATEWAY_NAME=mcp-gateway
            export MCP_LISTENER_HOST=mcp.example.com
            QAENV
            cat "{{ logdir }}/exports-17.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "17 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Register an MCP server with the gateway
              {{ r17.rc | default('?') }}
              {{ r17.stdout | default('') }}
              {{ r17.stderr | default('') }}
            dest: "{{ logdir }}/17.log"
    - block:
        - name: "18 | module-02-hands-on.adoc | Exercise 1 Register an MCP server with the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: gateway.networking.k8s.io/v1
            kind: HTTPRoute
            metadata:
              name: mcp-api-key-server-route
              namespace: ${MCP_SYSTEM}
            spec:
              parentRefs:
              - name: ${GATEWAY_NAME}
                namespace: ${GATEWAY_NS}
              hostnames:
              - api-key-server.mcp.local
              rules:
              - matches:
                - path:
                    type: PathPrefix
                    value: /
                backendRefs:
                - name: mcp-api-key-server
                  port: 9090
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r18
          changed_when: false
          failed_when:
            - r18.rc != 0
            - "'AlreadyExists' not in (r18.stderr | default(''))"
            - "'already exists' not in (r18.stderr | default(''))"
            - "'NotFound' not in (r18.stderr | default(''))"
      always:
        - name: "18 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Register an MCP server with the gateway
              {{ r18.rc | default('?') }}
              {{ r18.stdout | default('') }}
              {{ r18.stderr | default('') }}
            dest: "{{ logdir }}/18.log"
    - block:
        - name: "19 | module-02-hands-on.adoc | Exercise 1 Register an MCP server with the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: mcp.kuadrant.io/v1alpha1
            kind: MCPServerRegistration
            metadata:
              name: mcp-server-one
              namespace: ${MCP_SYSTEM}
            spec:
              prefix: serverone_
              targetRef:
                group: gateway.networking.k8s.io
                kind: HTTPRoute
                name: mcp-api-key-server-route
              credentialRef:
                name: mcp-server-one-secret
                key: api-key
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r19
          changed_when: false
          failed_when:
            - r19.rc != 0
            - "'AlreadyExists' not in (r19.stderr | default(''))"
            - "'already exists' not in (r19.stderr | default(''))"
            - "'NotFound' not in (r19.stderr | default(''))"
      always:
        - name: "19 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Register an MCP server with the gateway
              {{ r19.rc | default('?') }}
              {{ r19.stdout | default('') }}
              {{ r19.stderr | default('') }}
            dest: "{{ logdir }}/19.log"
    - block:
        - name: "20 | module-02-hands-on.adoc | Exercise 1 Register an MCP server with the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get mcpsr -n ${MCP_SYSTEM}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r20
          changed_when: false
          failed_when:
            - r20.rc != 0
            - "'AlreadyExists' not in (r20.stderr | default(''))"
            - "'already exists' not in (r20.stderr | default(''))"
            - "'NotFound' not in (r20.stderr | default(''))"
      always:
        - name: "20 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Register an MCP server with the gateway
              {{ r20.rc | default('?') }}
              {{ r20.stdout | default('') }}
              {{ r20.stderr | default('') }}
            dest: "{{ logdir }}/20.log"
    - block:
        - name: "21 | module-02-hands-on.adoc | Exercise 1 Register an MCP server with the gateway"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get mcpsr -n ${MCP_SYSTEM} -o yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r21
          changed_when: false
          failed_when:
            - r21.rc != 0
            - "'AlreadyExists' not in (r21.stderr | default(''))"
            - "'already exists' not in (r21.stderr | default(''))"
            - "'NotFound' not in (r21.stderr | default(''))"
      always:
        - name: "21 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Register an MCP server with the gateway
              {{ r21.rc | default('?') }}
              {{ r21.stdout | default('') }}
              {{ r21.stderr | default('') }}
            dest: "{{ logdir }}/21.log"
    - block:
        - name: "22 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -D /tmp/mcp_headers -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "Content-Type: application/json" \
              -d '{"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {"protocolVersion": "2025-11-25", "capabilities": {}, "clientInfo": {"name": "test-client", "version": "1.0.0"}}}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r22
          changed_when: false
          failed_when:
            - r22.rc != 0
            - "'AlreadyExists' not in (r22.stderr | default(''))"
            - "'already exists' not in (r22.stderr | default(''))"
            - "'NotFound' not in (r22.stderr | default(''))"
      always:
        - name: "22 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r22.rc | default('?') }}
              {{ r22.stdout | default('') }}
              {{ r22.stderr | default('') }}
            dest: "{{ logdir }}/22.log"
    - block:
        - name: "23 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            SESSION_ID=$(grep -i "mcp-session-id:" /tmp/mcp_headers | cut -d' ' -f2 | tr -d '\r')
            echo "MCP Session ID: $SESSION_ID"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r23
          changed_when: false
          failed_when:
            - r23.rc != 0
            - "'AlreadyExists' not in (r23.stderr | default(''))"
            - "'already exists' not in (r23.stderr | default(''))"
            - "'NotFound' not in (r23.stderr | default(''))"
        - name: "23 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-23.sh" <<'QAENV'
            SESSION_ID=$(grep -i "mcp-session-id:" /tmp/mcp_headers | cut -d' ' -f2 | tr -d '\r')
            QAENV
            cat "{{ logdir }}/exports-23.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "23 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r23.rc | default('?') }}
              {{ r23.stdout | default('') }}
              {{ r23.stderr | default('') }}
            dest: "{{ logdir }}/23.log"
    - block:
        - name: "24 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "Content-Type: application/json" \
              -H "mcp-session-id: $SESSION_ID" \
              -d '{"jsonrpc": "2.0", "id": 2, "method": "tools/list"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r24
          changed_when: false
          failed_when:
            - r24.rc != 0
            - "'AlreadyExists' not in (r24.stderr | default(''))"
            - "'already exists' not in (r24.stderr | default(''))"
            - "'NotFound' not in (r24.stderr | default(''))"
      always:
        - name: "24 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r24.rc | default('?') }}
              {{ r24.stdout | default('') }}
              {{ r24.stderr | default('') }}
            dest: "{{ logdir }}/24.log"
    - block:
        - name: "25 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "Content-Type: application/json" \
              -H "mcp-session-id: $SESSION_ID" \
              -d '{"jsonrpc": "2.0", "id": 3, "method": "prompts/list"}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r25
          changed_when: false
          failed_when:
            - r25.rc != 0
            - "'AlreadyExists' not in (r25.stderr | default(''))"
            - "'already exists' not in (r25.stderr | default(''))"
            - "'NotFound' not in (r25.stderr | default(''))"
      always:
        - name: "25 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r25.rc | default('?') }}
              {{ r25.stdout | default('') }}
              {{ r25.stderr | default('') }}
            dest: "{{ logdir }}/25.log"
    - block:
        - name: "26 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            rm -f /tmp/mcp_headers
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r26
          changed_when: false
          failed_when:
            - r26.rc != 0
            - "'AlreadyExists' not in (r26.stderr | default(''))"
            - "'already exists' not in (r26.stderr | default(''))"
            - "'NotFound' not in (r26.stderr | default(''))"
      always:
        - name: "26 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r26.rc | default('?') }}
              {{ r26.stdout | default('') }}
              {{ r26.stderr | default('') }}
            dest: "{{ logdir }}/26.log"
    - block:
        - name: "27 | module-02-hands-on.adoc | Exercise 2 Curate tools with a virtual MCP server"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            cat <<EOF | oc apply -f -
            apiVersion: mcp.kuadrant.io/v1alpha1
            kind: MCPVirtualServer
            metadata:
              name: dev-tools
              namespace: ${MCP_SYSTEM}
            spec:
              description: "Development and debugging tools"
              tools:
              - serverone_devtool1
              - serverone_headers1
              - serverone_debug1
            EOF
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r27
          changed_when: false
          failed_when:
            - r27.rc != 0
            - "'AlreadyExists' not in (r27.stderr | default(''))"
            - "'already exists' not in (r27.stderr | default(''))"
            - "'NotFound' not in (r27.stderr | default(''))"
      always:
        - name: "27 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Curate tools with a virtual MCP server
              {{ r27.rc | default('?') }}
              {{ r27.stdout | default('') }}
              {{ r27.stderr | default('') }}
            dest: "{{ logdir }}/27.log"
    - block:
        - name: "28 | module-02-hands-on.adoc | Exercise 2 Curate tools with a virtual MCP server"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get mcpvirtualserver -A
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r28
          changed_when: false
          failed_when:
            - r28.rc != 0
            - "'AlreadyExists' not in (r28.stderr | default(''))"
            - "'already exists' not in (r28.stderr | default(''))"
            - "'NotFound' not in (r28.stderr | default(''))"
      always:
        - name: "28 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Curate tools with a virtual MCP server
              {{ r28.rc | default('?') }}
              {{ r28.stdout | default('') }}
              {{ r28.stderr | default('') }}
            dest: "{{ logdir }}/28.log"
    - block:
        - name: "29 | module-02-hands-on.adoc | Exercise 2 Curate tools with a virtual MCP server"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -D /tmp/mcp_headers -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "Content-Type: application/json" \
              -d '{"jsonrpc": "2.0", "id": 1, "method": "initialize", "params": {"protocolVersion": "2025-11-25", "capabilities": {}, "clientInfo": {"name": "test-client", "version": "1.0.0"}}}'
            SESSION_ID=$(grep -i "mcp-session-id:" /tmp/mcp_headers | cut -d' ' -f2 | tr -d '\r')
            curl -s -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "Content-Type: application/json" \
              -H "mcp-session-id: $SESSION_ID" \
              -H "X-Mcp-Virtualserver: ${MCP_SYSTEM}/dev-tools" \
              -d '{"jsonrpc": "2.0", "id": 2, "method": "tools/list"}' | jq '.result.tools[].name'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r29
          changed_when: false
          failed_when:
            - r29.rc != 0
            - "'AlreadyExists' not in (r29.stderr | default(''))"
            - "'already exists' not in (r29.stderr | default(''))"
            - "'NotFound' not in (r29.stderr | default(''))"
        - name: "29 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-29.sh" <<'QAENV'
            SESSION_ID=$(grep -i "mcp-session-id:" /tmp/mcp_headers | cut -d' ' -f2 | tr -d '\r')
            QAENV
            cat "{{ logdir }}/exports-29.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "29 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Curate tools with a virtual MCP server
              {{ r29.rc | default('?') }}
              {{ r29.stdout | default('') }}
              {{ r29.stderr | default('') }}
            dest: "{{ logdir }}/29.log"
    - block:
        - name: "30 | module-02-hands-on.adoc | Exercise 2 Curate tools with a virtual MCP server"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s -X POST http://${MCP_LISTENER_HOST}:8080/mcp \
              -H "mcp-session-id: $SESSION_ID" \
              -H "Content-Type: application/json" \
              -d '{"jsonrpc": "2.0", "id": 3, "method": "tools/list"}' | jq '.result.tools[].name'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r30
          changed_when: false
          failed_when:
            - r30.rc != 0
            - "'AlreadyExists' not in (r30.stderr | default(''))"
            - "'already exists' not in (r30.stderr | default(''))"
            - "'NotFound' not in (r30.stderr | default(''))"
      always:
        - name: "30 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Curate tools with a virtual MCP server
              {{ r30.rc | default('?') }}
              {{ r30.stdout | default('') }}
              {{ r30.stderr | default('') }}
            dest: "{{ logdir }}/30.log"
    - block:
        - name: "31 | module-02-hands-on.adoc | Exercise 2 Curate tools with a virtual MCP server"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            rm -f /tmp/mcp_headers
            oc delete mcpvirtualserver dev-tools -n ${MCP_SYSTEM}
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r31
          changed_when: false
          failed_when:
            - r31.rc != 0
            - "'AlreadyExists' not in (r31.stderr | default(''))"
            - "'already exists' not in (r31.stderr | default(''))"
            - "'NotFound' not in (r31.stderr | default(''))"
      always:
        - name: "31 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Curate tools with a virtual MCP server
              {{ r31.rc | default('?') }}
              {{ r31.stdout | default('') }}
              {{ r31.stderr | default('') }}
            dest: "{{ logdir }}/31.log"
