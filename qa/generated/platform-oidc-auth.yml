# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: platform-oidc-auth (31 runnable blocks, 3 skipped)
- name: "Lab platform-oidc-auth"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/platform-oidc-auth/work"
    logdir: "{{ playbook_dir }}/../runs/platform-oidc-auth"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-getting-started.adoc | Exercise 1 Verify the OpenShift direct-OIDC prerequisites"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get authentication.config/cluster -o jsonpath='{.spec.type}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the OpenShift direct-OIDC prerequisites
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-getting-started.adoc | Exercise 1 Verify the OpenShift direct-OIDC prerequisites"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get authentication.config/cluster -o jsonpath='{.spec.oidcProviders[*].name}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the OpenShift direct-OIDC prerequisites
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-getting-started.adoc | Exercise 1 Verify the OpenShift direct-OIDC prerequisites"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get co kube-apiserver
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 1: Verify the OpenShift direct-OIDC prerequisites
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-getting-started.adoc | Exercise 2 Enable OIDC on the GatewayConfig CR"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc create secret generic idp-client-secret \
              --from-literal=clientSecret=$IDP_CLIENT_SECRET \
              -n openshift-ingress
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 2: Enable OIDC on the GatewayConfig CR
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-01-getting-started.adoc | Exercise 2 Enable OIDC on the GatewayConfig CR"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc patch gatewayconfig default-gateway --type='merge' -p='{
              "spec": {
                "oidc": {
                  "issuerURL": "'$IDP_ISSUER_URL'" <1>,
                  "clientID": "'$IDP_CLIENT_ID'" <2>,
                  "clientSecretRef": {
                    "name": "idp-client-secret" <3>,
                    "key": "clientSecret" <4>
                  }
                }
              }
            }'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Exercise 2: Enable OIDC on the GatewayConfig CR
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
    - block:
        - name: "09 | module-01-getting-started.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get secret idp-client-secret -n openshift-ingress
            oc get gatewayconfig default-gateway -o jsonpath='{.spec.oidc}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r09
          changed_when: false
          failed_when:
            - r09.rc != 0
            - "'AlreadyExists' not in (r09.stderr | default(''))"
            - "'already exists' not in (r09.stderr | default(''))"
            - "'NotFound' not in (r09.stderr | default(''))"
      always:
        - name: "09 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-getting-started.adoc — Verify
              {{ r09.rc | default('?') }}
              {{ r09.stdout | default('') }}
              {{ r09.stderr | default('') }}
            dest: "{{ logdir }}/09.log"
    - block:
        - name: "10 | module-02-hands-on.adoc | Exercise 1 Verify the OIDC flow end to end"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get consolelink
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r10
          changed_when: false
          failed_when:
            - r10.rc != 0
            - "'AlreadyExists' not in (r10.stderr | default(''))"
            - "'already exists' not in (r10.stderr | default(''))"
            - "'NotFound' not in (r10.stderr | default(''))"
      always:
        - name: "10 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Verify the OIDC flow end to end
              {{ r10.rc | default('?') }}
              {{ r10.stdout | default('') }}
              {{ r10.stderr | default('') }}
            dest: "{{ logdir }}/10.log"
    - block:
        - name: "11 | module-02-hands-on.adoc | Exercise 1 Verify the OIDC flow end to end"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gatewayconfig default-gateway -o yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r11
          changed_when: false
          failed_when:
            - r11.rc != 0
            - "'AlreadyExists' not in (r11.stderr | default(''))"
            - "'already exists' not in (r11.stderr | default(''))"
            - "'NotFound' not in (r11.stderr | default(''))"
      always:
        - name: "11 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Verify the OIDC flow end to end
              {{ r11.rc | default('?') }}
              {{ r11.stdout | default('') }}
              {{ r11.stderr | default('') }}
            dest: "{{ logdir }}/11.log"
    - block:
        - name: "12 | module-02-hands-on.adoc | Exercise 1 Verify the OIDC flow end to end"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get deployment kube-auth-proxy -n openshift-ingress
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r12
          changed_when: false
          failed_when:
            - r12.rc != 0
            - "'AlreadyExists' not in (r12.stderr | default(''))"
            - "'already exists' not in (r12.stderr | default(''))"
            - "'NotFound' not in (r12.stderr | default(''))"
      always:
        - name: "12 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Verify the OIDC flow end to end
              {{ r12.rc | default('?') }}
              {{ r12.stdout | default('') }}
              {{ r12.stderr | default('') }}
            dest: "{{ logdir }}/12.log"
    - block:
        - name: "13 | module-02-hands-on.adoc | Exercise 1 Verify the OIDC flow end to end"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gateway data-science-gateway -n openshift-ingress
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r13
          changed_when: false
          failed_when:
            - r13.rc != 0
            - "'AlreadyExists' not in (r13.stderr | default(''))"
            - "'already exists' not in (r13.stderr | default(''))"
            - "'NotFound' not in (r13.stderr | default(''))"
      always:
        - name: "13 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Verify the OIDC flow end to end
              {{ r13.rc | default('?') }}
              {{ r13.stdout | default('') }}
              {{ r13.stderr | default('') }}
            dest: "{{ logdir }}/13.log"
    - block:
        - name: "14 | module-02-hands-on.adoc | Exercise 1 Verify the OIDC flow end to end"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -s "$IDP_ISSUER_URL/.well-known/openid-configuration"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r14
          changed_when: false
          failed_when:
            - r14.rc != 0
            - "'AlreadyExists' not in (r14.stderr | default(''))"
            - "'already exists' not in (r14.stderr | default(''))"
            - "'NotFound' not in (r14.stderr | default(''))"
      always:
        - name: "14 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 1: Verify the OIDC flow end to end
              {{ r14.rc | default('?') }}
              {{ r14.stdout | default('') }}
              {{ r14.stderr | default('') }}
            dest: "{{ logdir }}/14.log"
    - block:
        - name: "15 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gatewayconfig default-gateway -o jsonpath='{.status.conditions[?(@.type=="Ready")].status}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r15
          changed_when: false
          failed_when:
            - r15.rc != 0
            - "'AlreadyExists' not in (r15.stderr | default(''))"
            - "'already exists' not in (r15.stderr | default(''))"
            - "'NotFound' not in (r15.stderr | default(''))"
      always:
        - name: "15 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r15.rc | default('?') }}
              {{ r15.stdout | default('') }}
              {{ r15.stderr | default('') }}
            dest: "{{ logdir }}/15.log"
    - block:
        - name: "16 | module-02-hands-on.adoc | Exercise 2 Map external IdP groups to OpenShift roles"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc apply -f odh-projects-read.yaml
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r16
          changed_when: false
          failed_when:
            - r16.rc != 0
            - "'AlreadyExists' not in (r16.stderr | default(''))"
            - "'already exists' not in (r16.stderr | default(''))"
            - "'NotFound' not in (r16.stderr | default(''))"
      always:
        - name: "16 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Map external IdP groups to OpenShift roles
              {{ r16.rc | default('?') }}
              {{ r16.stdout | default('') }}
              {{ r16.stderr | default('') }}
            dest: "{{ logdir }}/16.log"
    - block:
        - name: "17 | module-02-hands-on.adoc | Exercise 2 Map external IdP groups to OpenShift roles"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc adm policy add-cluster-role-to-group odh-projects-read odh-users
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r17
          changed_when: false
          failed_when:
            - r17.rc != 0
            - "'AlreadyExists' not in (r17.stderr | default(''))"
            - "'already exists' not in (r17.stderr | default(''))"
            - "'NotFound' not in (r17.stderr | default(''))"
      always:
        - name: "17 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Map external IdP groups to OpenShift roles
              {{ r17.rc | default('?') }}
              {{ r17.stdout | default('') }}
              {{ r17.stderr | default('') }}
            dest: "{{ logdir }}/17.log"
    - block:
        - name: "18 | module-02-hands-on.adoc | Exercise 2 Map external IdP groups to OpenShift roles"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc adm policy add-cluster-role-to-group self-provisioner odh-users
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r18
          changed_when: false
          failed_when:
            - r18.rc != 0
            - "'AlreadyExists' not in (r18.stderr | default(''))"
            - "'already exists' not in (r18.stderr | default(''))"
            - "'NotFound' not in (r18.stderr | default(''))"
      always:
        - name: "18 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 2: Map external IdP groups to OpenShift roles
              {{ r18.rc | default('?') }}
              {{ r18.stdout | default('') }}
              {{ r18.stderr | default('') }}
            dest: "{{ logdir }}/18.log"
    - block:
        - name: "19 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get clusterrolebinding odh-projects-read -o jsonpath='{.subjects[0].name}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r19
          changed_when: false
          failed_when:
            - r19.rc != 0
            - "'AlreadyExists' not in (r19.stderr | default(''))"
            - "'already exists' not in (r19.stderr | default(''))"
            - "'NotFound' not in (r19.stderr | default(''))"
      always:
        - name: "19 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r19.rc | default('?') }}
              {{ r19.stdout | default('') }}
              {{ r19.stderr | default('') }}
            dest: "{{ logdir }}/19.log"
    - block:
        - name: "20 | module-02-hands-on.adoc | Exercise 3 Troubleshoot a GatewayConfig that is not ready"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get gatewayconfig default-gateway -o yaml
            oc describe gatewayconfig default-gateway
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r20
          changed_when: false
          failed_when:
            - r20.rc != 0
            - "'AlreadyExists' not in (r20.stderr | default(''))"
            - "'already exists' not in (r20.stderr | default(''))"
            - "'NotFound' not in (r20.stderr | default(''))"
      always:
        - name: "20 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Troubleshoot a GatewayConfig that is not ready
              {{ r20.rc | default('?') }}
              {{ r20.stdout | default('') }}
              {{ r20.stderr | default('') }}
            dest: "{{ logdir }}/20.log"
    - block:
        - name: "21 | module-02-hands-on.adoc | Exercise 3 Troubleshoot a GatewayConfig that is not ready"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get secret idp-client-secret -n openshift-ingress
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r21
          changed_when: false
          failed_when:
            - r21.rc != 0
            - "'AlreadyExists' not in (r21.stderr | default(''))"
            - "'already exists' not in (r21.stderr | default(''))"
            - "'NotFound' not in (r21.stderr | default(''))"
      always:
        - name: "21 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Troubleshoot a GatewayConfig that is not ready
              {{ r21.rc | default('?') }}
              {{ r21.stdout | default('') }}
              {{ r21.stderr | default('') }}
            dest: "{{ logdir }}/21.log"
    - block:
        - name: "22 | module-02-hands-on.adoc | Exercise 3 Troubleshoot a GatewayConfig that is not ready"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -I "$IDP_ISSUER_URL/.well-known/openid-configuration"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r22
          changed_when: false
          failed_when:
            - r22.rc != 0
            - "'AlreadyExists' not in (r22.stderr | default(''))"
            - "'already exists' not in (r22.stderr | default(''))"
            - "'NotFound' not in (r22.stderr | default(''))"
      always:
        - name: "22 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Troubleshoot a GatewayConfig that is not ready
              {{ r22.rc | default('?') }}
              {{ r22.stdout | default('') }}
              {{ r22.stderr | default('') }}
            dest: "{{ logdir }}/22.log"
    - block:
        - name: "23 | module-02-hands-on.adoc | Exercise 3 Troubleshoot a GatewayConfig that is not ready"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc create secret generic oidc-ca-bundle \
              --from-file=ca.crt=/path/to/your-ca.crt \
              -n openshift-ingress
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r23
          changed_when: false
          failed_when:
            - r23.rc != 0
            - "'AlreadyExists' not in (r23.stderr | default(''))"
            - "'already exists' not in (r23.stderr | default(''))"
            - "'NotFound' not in (r23.stderr | default(''))"
      always:
        - name: "23 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Troubleshoot a GatewayConfig that is not ready
              {{ r23.rc | default('?') }}
              {{ r23.stdout | default('') }}
              {{ r23.stderr | default('') }}
            dest: "{{ logdir }}/23.log"
    - block:
        - name: "24 | module-02-hands-on.adoc | Exercise 3 Troubleshoot a GatewayConfig that is not ready"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get deployment kube-auth-proxy -n openshift-ingress
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r24
          changed_when: false
          failed_when:
            - r24.rc != 0
            - "'AlreadyExists' not in (r24.stderr | default(''))"
            - "'already exists' not in (r24.stderr | default(''))"
            - "'NotFound' not in (r24.stderr | default(''))"
      always:
        - name: "24 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 3: Troubleshoot a GatewayConfig that is not ready
              {{ r24.rc | default('?') }}
              {{ r24.stdout | default('') }}
              {{ r24.stderr | default('') }}
            dest: "{{ logdir }}/24.log"
    - block:
        - name: "25 | module-02-hands-on.adoc | Exercise 4 Access the gateway programmatically with a servic"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc create serviceaccount my-app-sa -n $SA_NAMESPACE
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r25
          changed_when: false
          failed_when:
            - r25.rc != 0
            - "'AlreadyExists' not in (r25.stderr | default(''))"
            - "'already exists' not in (r25.stderr | default(''))"
            - "'NotFound' not in (r25.stderr | default(''))"
      always:
        - name: "25 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Access the gateway programmatically with a service account token
              {{ r25.rc | default('?') }}
              {{ r25.stdout | default('') }}
              {{ r25.stderr | default('') }}
            dest: "{{ logdir }}/25.log"
    - block:
        - name: "26 | module-02-hands-on.adoc | Exercise 4 Access the gateway programmatically with a servic"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc create -f role.yaml -n $SA_NAMESPACE
            oc create rolebinding my-app-service-access \
              --role=my-app-service-access \
              --serviceaccount=${SA_NAMESPACE}:my-app-sa \
              -n $SA_NAMESPACE
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r26
          changed_when: false
          failed_when:
            - r26.rc != 0
            - "'AlreadyExists' not in (r26.stderr | default(''))"
            - "'already exists' not in (r26.stderr | default(''))"
            - "'NotFound' not in (r26.stderr | default(''))"
      always:
        - name: "26 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Access the gateway programmatically with a service account token
              {{ r26.rc | default('?') }}
              {{ r26.stdout | default('') }}
              {{ r26.stderr | default('') }}
            dest: "{{ logdir }}/26.log"
    - block:
        - name: "27 | module-02-hands-on.adoc | Exercise 4 Access the gateway programmatically with a servic"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            TOKEN=$(oc create token my-app-sa -n $SA_NAMESPACE --duration=1h)
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r27
          changed_when: false
          failed_when:
            - r27.rc != 0
            - "'AlreadyExists' not in (r27.stderr | default(''))"
            - "'already exists' not in (r27.stderr | default(''))"
            - "'NotFound' not in (r27.stderr | default(''))"
        - name: "27 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-27.sh" <<'QAENV'
            TOKEN=$(oc create token my-app-sa -n $SA_NAMESPACE --duration=1h)
            QAENV
            cat "{{ logdir }}/exports-27.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "27 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Access the gateway programmatically with a service account token
              {{ r27.rc | default('?') }}
              {{ r27.stdout | default('') }}
              {{ r27.stderr | default('') }}
            dest: "{{ logdir }}/27.log"
    - block:
        - name: "28 | module-02-hands-on.adoc | Exercise 4 Access the gateway programmatically with a servic"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            GATEWAY_URL=$(oc get route data-science-gateway -n openshift-ingress -o jsonpath='{.spec.host}')
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r28
          changed_when: false
          failed_when:
            - r28.rc != 0
            - "'AlreadyExists' not in (r28.stderr | default(''))"
            - "'already exists' not in (r28.stderr | default(''))"
            - "'NotFound' not in (r28.stderr | default(''))"
        - name: "28 | persist exports"
          ansible.builtin.shell: |
            cat > "{{ logdir }}/exports-28.sh" <<'QAENV'
            GATEWAY_URL=$(oc get route data-science-gateway -n openshift-ingress -o jsonpath='{.spec.host}')
            QAENV
            cat "{{ logdir }}/exports-28.sh" >> "{{ logdir }}/env"
          changed_when: false
      always:
        - name: "28 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Access the gateway programmatically with a service account token
              {{ r28.rc | default('?') }}
              {{ r28.stdout | default('') }}
              {{ r28.stderr | default('') }}
            dest: "{{ logdir }}/28.log"
    - block:
        - name: "29 | module-02-hands-on.adoc | Exercise 4 Access the gateway programmatically with a servic"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -k -H "Authorization: Bearer ${TOKEN}" \
              "https://${GATEWAY_URL}/your-service-path"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r29
          changed_when: false
          failed_when:
            - r29.rc != 0
            - "'AlreadyExists' not in (r29.stderr | default(''))"
            - "'already exists' not in (r29.stderr | default(''))"
            - "'NotFound' not in (r29.stderr | default(''))"
      always:
        - name: "29 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Exercise 4: Access the gateway programmatically with a service account token
              {{ r29.rc | default('?') }}
              {{ r29.stdout | default('') }}
              {{ r29.stderr | default('') }}
            dest: "{{ logdir }}/29.log"
    - block:
        - name: "30 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -k -i -H "Authorization: Bearer ${TOKEN}" \
              "https://${GATEWAY_URL}/your-service-path"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r30
          changed_when: false
          failed_when:
            - r30.rc != 0
            - "'AlreadyExists' not in (r30.stderr | default(''))"
            - "'already exists' not in (r30.stderr | default(''))"
            - "'NotFound' not in (r30.stderr | default(''))"
      always:
        - name: "30 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r30.rc | default('?') }}
              {{ r30.stdout | default('') }}
              {{ r30.stderr | default('') }}
            dest: "{{ logdir }}/30.log"
    - block:
        - name: "31 | module-02-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            curl -k -i "https://${GATEWAY_URL}/your-service-path"
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r31
          changed_when: false
          failed_when:
            - r31.rc != 0
            - "'AlreadyExists' not in (r31.stderr | default(''))"
            - "'already exists' not in (r31.stderr | default(''))"
            - "'NotFound' not in (r31.stderr | default(''))"
      always:
        - name: "31 | save log"
          ansible.builtin.copy:
            content: |
              # module-02-hands-on.adoc — Verify
              {{ r31.rc | default('?') }}
              {{ r31.stdout | default('') }}
              {{ r31.stderr | default('') }}
            dest: "{{ logdir }}/31.log"
