# GENERATED by qa/tools/extract-exec-blocks.py — do not edit; re-run the extractor.
# Lab: view-agent-deployments (8 runnable blocks, 1 skipped)
- name: "Lab view-agent-deployments"
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    workdir: "{{ playbook_dir }}/../runs/view-agent-deployments/work"
    logdir: "{{ playbook_dir }}/../runs/view-agent-deployments"
  environment:
    KUBECONFIG: "{{ lookup('env', 'KUBECONFIG') }}"
  tasks:
    - name: "prep workdir"
      ansible.builtin.file:
        path: "{{ item }}"
        state: directory
        mode: "0755"
      loop:
        - "{{ workdir }}"
        - "{{ logdir }}"
    - name: "prep env file"
      ansible.builtin.copy:
        content: ""
        dest: "{{ logdir }}/env"
        force: false
        mode: "0644"
    - block:
        - name: "01 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc whoami
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r01
          changed_when: false
          failed_when:
            - r01.rc != 0
            - "'AlreadyExists' not in (r01.stderr | default(''))"
            - "'already exists' not in (r01.stderr | default(''))"
            - "'NotFound' not in (r01.stderr | default(''))"
      always:
        - name: "01 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r01.rc | default('?') }}
              {{ r01.stdout | default('') }}
              {{ r01.stderr | default('') }}
            dest: "{{ logdir }}/01.log"
    - block:
        - name: "02 | getting-connected.adoc | Exercise 2 Create your working project"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc new-project abc123-user1
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r02
          changed_when: false
          failed_when:
            - r02.rc != 0
            - "'AlreadyExists' not in (r02.stderr | default(''))"
            - "'already exists' not in (r02.stderr | default(''))"
            - "'NotFound' not in (r02.stderr | default(''))"
      always:
        - name: "02 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Exercise 2: Create your working project
              {{ r02.rc | default('?') }}
              {{ r02.stdout | default('') }}
              {{ r02.stderr | default('') }}
            dest: "{{ logdir }}/02.log"
    - block:
        - name: "03 | getting-connected.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc project
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r03
          changed_when: false
          failed_when:
            - r03.rc != 0
            - "'AlreadyExists' not in (r03.stderr | default(''))"
            - "'already exists' not in (r03.stderr | default(''))"
            - "'NotFound' not in (r03.stderr | default(''))"
      always:
        - name: "03 | save log"
          ansible.builtin.copy:
            content: |
              # getting-connected.adoc — Verify
              {{ r03.rc | default('?') }}
              {{ r03.stdout | default('') }}
              {{ r03.stderr | default('') }}
            dest: "{{ logdir }}/03.log"
    - block:
        - name: "04 | module-01-hands-on.adoc | Exercise 1 Check and enable the agentOps feature flag"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get odhdashboardconfig odh-dashboard-config -n redhat-ods-applications -o jsonpath='{.spec.dashboardConfig.agentOps}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r04
          changed_when: false
          failed_when:
            - r04.rc != 0
            - "'AlreadyExists' not in (r04.stderr | default(''))"
            - "'already exists' not in (r04.stderr | default(''))"
            - "'NotFound' not in (r04.stderr | default(''))"
      always:
        - name: "04 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-hands-on.adoc — Exercise 1: Check and enable the agentOps feature flag
              {{ r04.rc | default('?') }}
              {{ r04.stdout | default('') }}
              {{ r04.stderr | default('') }}
            dest: "{{ logdir }}/04.log"
    - block:
        - name: "05 | module-01-hands-on.adoc | Exercise 1 Check and enable the agentOps feature flag"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc patch odhdashboardconfig odh-dashboard-config -n redhat-ods-applications --type=merge -p '{"spec":{"dashboardConfig":{"agentOps":true}}}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r05
          changed_when: false
          failed_when:
            - r05.rc != 0
            - "'AlreadyExists' not in (r05.stderr | default(''))"
            - "'already exists' not in (r05.stderr | default(''))"
            - "'NotFound' not in (r05.stderr | default(''))"
      always:
        - name: "05 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-hands-on.adoc — Exercise 1: Check and enable the agentOps feature flag
              {{ r05.rc | default('?') }}
              {{ r05.stdout | default('') }}
              {{ r05.stderr | default('') }}
            dest: "{{ logdir }}/05.log"
    - block:
        - name: "06 | module-01-hands-on.adoc | Verify"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get odhdashboardconfig odh-dashboard-config -n redhat-ods-applications -o jsonpath='{.spec.dashboardConfig.agentOps}'
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r06
          changed_when: false
          failed_when:
            - r06.rc != 0
            - "'AlreadyExists' not in (r06.stderr | default(''))"
            - "'already exists' not in (r06.stderr | default(''))"
            - "'NotFound' not in (r06.stderr | default(''))"
      always:
        - name: "06 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-hands-on.adoc — Verify
              {{ r06.rc | default('?') }}
              {{ r06.stdout | default('') }}
              {{ r06.stderr | default('') }}
            dest: "{{ logdir }}/06.log"
    - block:
        - name: "07 | module-01-hands-on.adoc | Exercise 3 Trace the Sandbox CRs behind the list"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc api-resources | grep -i sandbox
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r07
          changed_when: false
          failed_when:
            - r07.rc != 0
            - "'AlreadyExists' not in (r07.stderr | default(''))"
            - "'already exists' not in (r07.stderr | default(''))"
            - "'NotFound' not in (r07.stderr | default(''))"
      always:
        - name: "07 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-hands-on.adoc — Exercise 3: Trace the Sandbox CRs behind the list
              {{ r07.rc | default('?') }}
              {{ r07.stdout | default('') }}
              {{ r07.stderr | default('') }}
            dest: "{{ logdir }}/07.log"
    - block:
        - name: "08 | module-01-hands-on.adoc | Exercise 3 Trace the Sandbox CRs behind the list"
          ansible.builtin.shell: |
            set -a
            [ -f "{{ logdir }}/env" ] && . "{{ logdir }}/env"
            set +a
            oc get sandboxes -A
          args:
            executable: /bin/bash
            chdir: "{{ workdir }}"
          register: r08
          changed_when: false
          failed_when:
            - r08.rc != 0
            - "'AlreadyExists' not in (r08.stderr | default(''))"
            - "'already exists' not in (r08.stderr | default(''))"
            - "'NotFound' not in (r08.stderr | default(''))"
      always:
        - name: "08 | save log"
          ansible.builtin.copy:
            content: |
              # module-01-hands-on.adoc — Exercise 3: Trace the Sandbox CRs behind the list
              {{ r08.rc | default('?') }}
              {{ r08.stdout | default('') }}
              {{ r08.stderr | default('') }}
            dest: "{{ logdir }}/08.log"
